# Handoff programme — tracking board

**What this file is.** The single status record for the nine Claude CLI prompts written by the
2026-08-21 status review (`docs/reviews/status-review-2026-08-21.md`). It answers three
questions in one table: *which plan items does each prompt own*, *where does each prompt
stand*, and *where is the evidence*.

**What it is not.** `development-plans/PROGRESS.md` stays the canonical execution tracker —
phase status, acceptance evidence, the Decisions log and the Human review queue live there
and nowhere else. This board never restates that evidence; it points at it. The two do not
compete: PROGRESS says what is true of the product; this board says what each prompt is
doing about it.

**Rules (every prompt follows them; the prompts' own text says so):**

1. A prompt updates **its own row only**, twice: `🔵` in its first commit on the branch;
   `✅` after the merge, in one small `docs(tracking): …` commit made directly on `main`
   and pushed (docs only — `docs/**` never deploys). Never another prompt's row.
2. The row carries the branch, the PR number and merge sha, the archived prompt path, and
   the PROGRESS anchor (section heading or queue-row date + title) where the evidence is.
3. A prompt that stops early sets `⛔` with what remains, so the next session does not
   re-derive it.
4. If a prompt's "Related plan items" column turns out to be wrong while executing, fix
   the column in the same commit — the column is the contract for what "done" means.

**Status values:** ⬜ not started · 🔵 in progress — `<branch>`, `<date>` · ✅ merged — PR #n
(`<sha>`), `<date>`, archived at `<path>` · ⛔ stopped — `<reason>`, remaining: `<items>`

**Programme complete, 2026-08-25.** All nine rows are ✅ (PRs #21–#42, with the follow-up
PRs each row names). A post-programme adversarial review (PROGRESS session log,
2026-08-25) re-verified every row against the tree and production and filed five LOW
findings; row 10 below lands them.

## Recommended order

1 → 2 → **9** → 3 → 4 → 5 → 6 → 7 → 8. Prompt 9 (the H1 seal proof) depends only on 2, changes
no product code and gates launch, so it is pulled forward. 3 and 4 are frontend and backend
respectively and *could* run in parallel on two branches, but both edit `PROGRESS.md` and
share the one local stack — run them one after the other. Never start a prompt while another
one's branch is unmerged.

## The board

| # | Prompt | Related plan items it owns (PROGRESS anchors unless stated) | Status | Branch · PR / merge · archived at | Evidence |
|---|---|---|---|---|---|
| 1 | `handoff-to-cli-docs-reconciliation.md` | Status review §3.1 items 1–10, 19–23 and §3.2 11–18 (record-only fixes); `development-plans/README.md` status line + Phase-12 row; phase docs 00–09/12 executed footers; prompts 1/2b/3 banners; `01-architecture.md` §2/§4/§17/§19; design contract grounding (:5/:314) + amendment log; `docs/ops/*` Railway-ization + H2/H3 closure; QA report / remediation prompt historical banners; `10-launch-checklist.md` evidence notes; `user-guide.md` guest caps; **commits the untracked `2026-08-21-phase-12-production-audit.md`**; adds the queue rows for §3.2 #11–14 and #24 | ✅ merged — PR #22 (`dc546f0`), 2026-08-22, archived at `docs/archived/2026-08-22-handoff-to-cli-docs-reconciliation.md` | `docs/reconcile-2026-08-21` · PR #22 · `dc546f0` | PROGRESS session log **"2026-08-22 — Record reconciliation: making the docs say what the code does"**, whose close-out table maps every review §3/§6 item to the commit that fixed it. Queue rows **closed**: "Self-serve account deletion" (✔ resolved 2026-08-02, Phase 10) and the design-contract grounding row (opened and closed in the same change). Queue rows **added**: version-Undo/Redo cursor committed before the revert; `NUM_PROXIES` three values; `@api_unavailable` drops the header set; esign suite coupled to `/certs/`; gate green with Gotenberg skipped. Queue rows **corrected, still open**: `base_version_seq` (mechanism re-pointed at `workspace.html`'s bindings), the panels row (widened to own the whole `takeUntilDestroyed` sweep), node-25 `localStorage` (widened — it blocks host-side gates today) |
| 2 | `handoff-to-cli-e2e-gate-hardening.md` | Queue rows: "`typeof localStorage === 'undefined'` is no longer a safe test" (2026-08-21); "Nothing still asserts that a page actually drew" (2026-08-20); "The e2e suite can silently test stale backend code" (2026-08-21); "A stale `base_version_seq` after every save" (2026-08-02); "The suites are memory-sensitive… `phase-3`/`phase-4`" (2026-08-02); review §3.2 #13 (esign cert path) and #14 (gate passes with Gotenberg skipped); Phase 10 §10.5 `@quarantine`; `01-architecture.md` §18 amendment | ✅ merged — PR #23 (`e0ead5b`), 2026-08-22, archived at `docs/archived/2026-08-22-handoff-to-cli-e2e-gate-hardening.md` | `fix/e2e-gate-hardening` · PR #23 · `e0ead5b` (+ follow-up PR #24 `b40d863`, the sampler correction) | PROGRESS session log **"2026-08-22 — E2E and gate hardening"**. Queue rows **closed**: node-25 `localStorage` (2026-08-21) · "Nothing still asserts that a page actually drew" (2026-08-20) · "The e2e suite can silently test stale backend code" (2026-08-21) · "A stale `base_version_seq` after every save" (2026-08-02) · the `phase-3`/`phase-4` half of "The suites are memory-sensitive" (2026-08-02; the `uploadFiles` half stays open, now with a lever) · esign cert path (§3.2 #13) · gate green with Gotenberg skipped (§3.2 #14). Queue rows **added**: the annotate autosave's stale `base_version_seq`; Postgres's connection budget under a long test session; `phase-12`'s nudge assertion meeting an unsettled frame. Decisions log **+7**; `01-architecture.md` §18 gains the three rules |
| 3 | `handoff-to-cli-workspace-debt-batch.md` | Queue rows: "The five remaining workspace panels' job subscriptions…" (2026-08-04, widened to the strays in `tool-page.ts`/`dashboard.ts`); "L9 gives thumbnails a manual retry but no backoff" (2026-08-04); the version-Undo/Redo-cursor row added by prompt 1 (review §3.2 #24); the 2026-08-21 report's `image_stamp` palette and "4 of 2 page(s) differ" observations; Phase 12 D11 hardening | ✅ merged — PR #27 (`d7978c7`), 2026-08-23, archived at `docs/archived/2026-08-23-handoff-to-cli-workspace-debt-batch.md` | `fix/workspace-debt-2026-08` · PR #27 · `d7978c7` | PROGRESS session log **"2026-08-23 (later still) — Workspace debt: five things the record had been carrying"**, with the browser evidence in `docs/reviews/evidence/workspace-debt/` (indexed by its own `README.md`). Queue rows **closed**: the job-subscription row (2026-08-04) — which understated it, the audit finding **72 bare subscriptions across 24 files**, now all piped and held by a custom ESLint rule *and* runtime assertions on rxjs's `observed`; "L9 gives thumbnails a manual retry but no backoff" (2026-08-04) — 429/503 back off with a rail-wide shared pause, measured in the browser as 12 refused tiles all staying in their **loading** state and filling by themselves at ~47 s; the version-cursor row (2026-08-22, review §3.2 #24) — committed on success only, and a throttled Redo now leaves the bar reading exactly what it read before. Queue rows **added**: the `phase-10-a11y` ceremony-keyboard spec losing its session (seen once, green in isolation and on a second full run, mechanism unpinned); "Operation failed" being the whole of what a refused operation says. Queue row **extended**: the `web` dev server starving the frontend-unit leg gains its **third** measurement. Decisions log **+6**; design contract amended at §3 (twice — a new **Tool palette button** spec and the three **thumbnail-tile states**), §4 and the grounding list, with the amendment log rows to match. Follow-up on `main`: `1e1919a` removes a virtiofs `tsconfig.app 2.json` this PR swept in and ignores the shape |
| 4 | `handoff-to-cli-backend-debt-batch.md` | Queue rows: "`record_password_failure`'s incr-then-set fallback" (2026-08-04); "The concurrency-slot race is closed on Postgres and unprovable on SQLite" (2026-08-04); "`usage_recompute` has never existed" (2026-08-02); "A completed signature can silently fail to land on the source document" (2026-08-02); "Account-side cleanup of `uploads/…` image assets" (2026-08-01); "A document under a signature request outlives the 30-day trash promise" (2026-08-02, the UI half); `NUM_PROXIES` and `@api_unavailable` rows added by prompt 1 (review §3.2 #11–12); `01-architecture.md` §9/§15/§19 | ✅ merged — PR #28 (`757eeea`), 2026-08-23, archived at `docs/archived/2026-08-23-handoff-to-cli-backend-debt-batch.md` | `fix/backend-debt-2026-08` · PR #28 · `757eeea` | PROGRESS session log **"2026-08-23 (later still, backend) — Backend debt: counters that could lie, an append nobody was told about, a reconciler that never existed"**, with the browser evidence in `docs/reviews/evidence/backend-debt/` (indexed by its own `README.md`). Queue rows **closed**: `record_password_failure`'s incr-then-set fallback (2026-08-04) — `add`+`incr`, and a stub whose `set` overwrites proves two cold misses now read 2; the concurrency-slot race (2026-08-04) — staged across two threads and two connections, 20 iterations, and shown to fail (`202 != 429`) with the lock removed; `usage_recompute` has never existed (2026-08-02) — it does now, at 02:00, agreeing with claim, both purges and the export GC, and reporting **0 drift across 37 production principals** on its first real run; a completed signature failing silently (2026-08-02) — `source_append_error`, surfaced as a §3 notice and cleared on a successful resume, with `phase-8` asserting it is **absent** on the happy path; account-side `uploads/…` cleanup (2026-08-01) — `account_assets_purge`, **with saved signatures exempt by key, which contradicts the prompt that commissioned it**; `NUM_PROXIES` three different numbers (2026-08-22) — 3 baked into the Railway image, held by a test that parses the Dockerfile; `@api_unavailable` dropping the header set (2026-08-22) — both confs, proven against real nginx because the prescribed `curl` at :4200 hits the dev server. Queue row **closed that was not on this prompt's list**: "The `web` dev server starves the frontend-unit leg" (2026-08-22) — occurrences four and five were this branch's own gate, so the one-line fix it had been asking for through three prompts is now in `test.sh`, with the mechanism measured causally (transform 98.9 s → 6.3 s). Queue row **extended, still open by design**: a document under a signature request outliving the 30-day trash promise (2026-08-02) — the UI half landed (`has_sign_requests`, no dead "Delete forever", an inline explanation), and the product decision stays with the owner. Queue row **added**: the dashboard keeps its desktop sidebar beside a two-column grid at 390 px. Decisions log **+5**; `01-architecture.md` §15/§19 amended; design contract §3 gains **Inline notice (`.notice`)** with both-mode contrast measured, plus its amendment-log row |
| 5 | `handoff-to-cli-phase-11-adsense-review.md` | **Phase 11** status-table row (→ 🔵/🟠 "awaiting owner: domain", never ✅ from this prompt); `phase-11-adsense-review.md` 11B, 11C, the floors, the tool-page top-up, 11A parameterised; design contract at four sites; `01-architecture.md` §21.6; `docs/09-adsense-readiness.md` content item; `docs/ops/domain-cutover.md` (new) | ✅ merged — PR #30 (`db66806`), 2026-08-24, archived at `docs/archived/2026-08-24-handoff-to-cli-phase-11-adsense-review.md` | `feat/phase-11-guides-and-contact` · PR #30 · `db66806` | PROGRESS session log **"2026-08-24 — Phase 11, the half that needs no domain: contact, identity, twelve guides and the content floors"**, with the browser evidence in `docs/reviews/evidence/phase-11/` (indexed by its own `README.md`). **Phase 11 row → 🔵, deliberately not ✅** — six of its eleven criteria are owner or calendar work and each carries a one-line reason; the terminal criterion is external (Google). Landed: `/contact` (prerendered, in the sitemap, footer-linked, no form — SMTP is off) · the About identity paragraph · **12 guides + `/guides`**, 10,560 words, `Article` JSON-LD, every claim checked against `TIERS`, `core/retention.ts` and the engine modules · all 24 tool pages to a **250-word / 4-FAQ** floor, additively (64 insertions, 0 deletions) · **43 prerendered routes and 43 `<loc>`s** · 11A parameterised, with the nginx redirect block commented out and its ordering warning **measured against real nginx** (wrong order 301-loops the apex *and* the healthcheck, failing every deploy) · `docs/ops/domain-cutover.md`. Contract amended at all four sites **before** the UI, plus the §11 log row — and the log's newest row was reattached to its table, having been rendering as literal pipes. Decisions log **+8**, three of which are reversals of this branch's own work: the merge-bookmark claim that was **false in the live `/merge-pdf` copy since Phase 2B** and copied into a guide before checking (measured: the merged outline is fully nested and internal links survive renumbered); two design-contract sentences the browser disproved; and a 40-character FAQ-answer floor removed because it failed seven answers that were right as they stood. Gate: `ruff`+`mypy` clean · pytest **1159 passed, 6 skipped** · `ng test` **508/58** (from 466) · Playwright **74 passed, 1 skipped** of 75 (the skip is the 404-status test `ng serve` cannot express — run separately against real nginx, where it passes) · Lighthouse **100/100/100** on landing, a tool page and a guide. Production verified after the deploy: `/contact`, `/guides` and the guides all **200**, `/guides/does-not-exist` **404**, sitemap **43 `<loc>`s**, seven footer links, `Article` JSON-LD and **17 paragraphs of prose in the served HTML**. Queue rows **added**: none. |
| 6 | `handoff-to-cli-mobile-workspace.md` | Queue row "The workspace on a phone is stacked, not designed" (2026-08-20); design contract §3/§4 phone-workspace spec (new); Phase 12 D8 rail-reachability on touch. **Not this prompt** (recorded 2026-08-24 rather than silently widened): the 2026-08-23 queue row "the dashboard keeps its desktop sidebar beside a two-column grid at 390 px" proposes widening this column to `/app/dashboard` and the app shell. This prompt's text is scoped to `/app/doc/:id` throughout — §5's checks, §3's spec name and its gate all name the workspace — so the dashboard half stays open with no owner rather than being half-done here | ✅ merged — PR #31 (`7650ab2`), 2026-08-24, archived at `docs/archived/2026-08-24-handoff-to-cli-mobile-workspace.md` | `feat/mobile-workspace` · PR #31 · `7650ab2` | PROGRESS session log **"2026-08-24 (later) — A designed phone workspace: drawers and a bottom bar"**, with the browser evidence in `docs/reviews/evidence/mobile-workspace/` (indexed by its own `README.md`). Queue row **closed**: "The workspace on a phone is stacked, not designed" (2026-08-20) — the rails are **bottom sheets** (one at a time, grip + title + 44 px close, Escape and scrim, CDK focus trap, body scroll locked, `translateY` only so reduced motion loses nothing but the slide), the nine modes a **persistent bottom bar** carrying an opener per rail and, docked at its end, the mode's own Undo/Redo and its primary, and the **page is first** at fit-to-width with the pane owning the full width. The workspace bar shrinks to back · title · meta · ⋯ · toggle, its six-button cluster moving into a **More** sheet as *one* set of elements through one `<ng-template>` — so there is still exactly one `[data-test=download]` in the DOM at any width, which a unit case asserts for all fifteen bar `data-test`s at both widths. Design contract amended **before** the code at §2 (`--shadow-up-3`), §3 (a new **Phone workspace** component), §3 headers, §4, §10 and the §11 log — whose *"pending, not yet sanctioned"* list this **emptied**, three days after it was written; three of its sentences were corrected afterwards by what the implementation settled, in their own commit. **Desktop unchanged, measured rather than eyeballed**: the same geometry script over all nine modes with `main`'s `frontend/src` checked out and then the branch's — **0 differences at 768 px, 0 at 1280 px**. **Four defects the browser found that no test had**: the column growing to **1348 px against 844** in the three 900 px-render modes, taking the bar off the bottom (the sideways assertion stayed green, because a scrollbar narrows both of its sides); the **More sheet shipping with no head**; touch targets sized for a desk, worst of them the comment row's D8 actions at **16 px**; and a focus ring clipped by the sheet's own scroller. Gate: `ruff`+`mypy` clean · pytest **1159 passed, 6 skipped**, coverage apps **91.53 %** / pdf_engine **91.87 %** · `ng lint` clean · `ng test` **62 files / 535 tests** (from 58 / 508) · build + `verify:prerender` clean · Playwright **79 passed, 1 skipped of 80**, including the new `phase-10-mobile.spec.ts` at an exact 390 × 844 with device emulation **off**. Lighthouse accessibility at the mobile preset, reported honestly: **100** on `?mode=annotate` — this layout's own markup — and **86** on the View route against **87** on `main`, the same three vendor `pdf-shy-button` / `#primaryZoom*` audits failing on both, with the one-point gap traced to `image-alt` leaving the denominator because the thumbnails now sit in a closed sheet (proven by a snapshot audit with the drawer open). **Production verified after the deploy**: nine modes at 390 × 844 as a guest, `scrollWidth === visualViewport.width === 390` and the bar's bottom edge at 844 in every one, focus trapped and returned, one Download in the document, console clean. Queue rows **added**: the `dir="rtl"` ~10 000 px document overflow that belongs to `ngx-extended-pdf-viewer`'s live region and its inline `left: -10000px`; three number/select fields staying 36 px inside a sheet because `!min-h-9` can only be answered by another `!important`; and the annotate zoom seed reading `innerWidth` where it means the layout viewport. Decisions log **+9**, one of them the unit runner's per-test budget — the gate went red twice on a spec this branch never touches, and the measurement (468 ms alone → 3 110 ms in the full suite → over 5 000 in the gate) says the default was measuring the machine. |
| 7 | `handoff-to-cli-type-aware-eslint.md` | Queue row "Type-aware ESLint" (2026-08-02); Decisions-log follow-through of "Type-aware ESLint is deliberately not in this change" (2026-08-02); `01-architecture.md` §18 CI note | ✅ merged — PR #38 (`671fb08`), 2026-08-24, archived at `docs/archived/2026-08-24-handoff-to-cli-type-aware-eslint.md` | `chore/type-aware-eslint` · PR #38 · `671fb08` | PROGRESS session log **"2026-08-24 (last, part four) — Type-aware ESLint: the number was wrong, and so was its root cause"**, with the browser evidence in `docs/reviews/evidence/type-aware-eslint/` (indexed by its own `README.md`). Queue row **closed**: "Type-aware ESLint" (2026-08-02) — and **both of its headline numbers were wrong on today's tree while both of its throwaway ones were exact**: **344** findings under `recommendedTypeChecked` and **894** under `strictTypeChecked`, not 198, but `no-floating-promises` **14** and `no-misused-promises` **3**, to the finding. The root cause the row names is no longer the largest: **246 of the 344 are in spec files**, and **199 of those trace to Angular typing its own test surfaces `any`** — `ComponentFixture.nativeElement` (126), `TestRequest.request.body` (67), `DebugElement.nativeElement` (6), attributed by reading the source line at every one with none left over; the `HttpErrorResponse.error` family is **63** in app code. `core/api-error.ts` now reads the §6 envelope once for all **28** call sites, retiring four spellings of the same walk — one of which (`ToolPage.fail`) *declared* its parameter as the envelope shape and was handed an `HttpErrorResponse` at all six of its call sites, which type-checked because `any` satisfies anything. **Two defects this change made itself, neither caught by the compiler:** the helper first gated on `instanceof HttpErrorResponse` and collapsed three workspace error screens to "We could not reach ZenPDF" — the prompt's own "narrowed error that now hides a message", caught by `workspace-error.spec.ts`, which throws plain object literals and was entitled to; and `non-nullable-type-assertion-style`'s **auto-fix** rewrote `querySelector(…) as HTMLElement` to `querySelector(…)!`, which is `Element` and has no `.style`, caught by the build. **Thirteen app-code findings were one mistake in four files** — `inject(ElementRef<HTMLElement>)` reads as though it types the host and does not, probed against the compiler. **Two of the prompt's instructions were not followed, each with the measurement written down:** `void` *is* the answer for thirteen of the fourteen floating navigations, because `provideBrowserGlobalErrorListeners()` already routes `unhandledrejection` to `ClientErrorHandler` and a `.catch()` would suppress the only report that exists (the fourteenth, `accountGuard`, was a real defect and returns a `UrlTree` now); and §5 step 1's countdown renders on the **first** refusal only. Decisions log **+7**; `01-architecture.md` §18 CI note gains `eslint` **(type-aware)** and what it costs a CI. Queue row **added**: "A guest who keeps retrying stops being told how long to wait" — pre-existing, backend, `SimpleRateThrottle.wait()` returning `None`. Gate: `ruff`+`mypy` clean · pytest **1159 passed, 6 skipped**, coverage apps **91.53 %** / pdf_engine **91.87 %** · `ng lint` clean and type-aware in the gate at **+5.5 s** (11.5 → 17 s in the project's own container) · `ng test` **65 files / 575 tests** (from 64/570) · build + `verify:prerender` green at 43 routes · Playwright **86 passed, 1 skipped of 87**. Bundle: `main-*.js` **+0.196 %**, initial total **+0.12 %**, all JS **+0.035 %** — inside the ±2 %. **Production verified after the deploy**, with the deploy confirmed by the bundle hash (`main-R6WL3ZXN.js` → `main-XNN7UJ7I.js`, the same name the local production build produced) rather than by a clock |
| 8 | `handoff-to-cli-launch-gate-evidence.md` | Phase 10 acceptance criteria 2 (Lighthouse), 4 (three consecutive full runs), 6 (p95) — `[~]` → evidenced; queue rows "Lighthouse on the deployed prod build", "`@full` suite, three consecutive nightly runs", "Load test (`locust`) and p95 budgets", "The p95 number itself" (all 2026-08-02); `docs/10-launch-checklist.md` "Final" evidence notes; `docs/ops/release.md` | ✅ merged — PR #41 (`cb82442`), 2026-08-25, archived at `docs/archived/2026-08-25-handoff-to-cli-launch-gate-evidence.md` | `docs/launch-gate-evidence` · PR #41 · `cb82442` | PROGRESS session log **"2026-08-25 (later still) — Launch-gate evidence: Lighthouse, three recorded runs, `@smoke` against production, and the p95 that is still owed"**, with the full write-up in `docs/reviews/evidence/launch-gate-2026-08-25.md` and browser evidence in `docs/reviews/evidence/launch-gate/`. **Lighthouse on the deployed build**: landing **100/100/100/100** desktop, **94/100/100/100** mobile, a tool page and a guide **100 across all four**. The dashboard cannot be run against the host without logging a real account into the live service, so it was measured on a local **production build** behind this repo's own `nginx.conf` — **96/96/100/69** — where the dev-server run's 65 for performance is kept only to show why that number is worthless (Lighthouse's `modern-http` insight puts 1,030 ms of it on HTTP/1.1). **Two criteria still cannot be ticked and the reasons are the finding**: the dashboard's SEO 69 is the product being *correct* (`is-crawlable` on a deliberately-noindex route — the box as written would need the product to get worse), and the three-runs criterion is `[~]` not `[x]` because "nightly" and "prod-shaped" are both unmet, which this prompt's own honesty self-review caught **after** it had been written `[x]`. Criteria updated: 2 (Lighthouse) `[~]` with the measured numbers, 4 (three runs) `[~]` with both gaps named, 6 (p95) `[~]` with the local figure. Queue rows **closed**: "Lighthouse on the deployed prod build" · "`@full` suite, three consecutive nightly runs" (its stated reason retired — the cadence has been run, costs ~8m30s, green 3×) · "Load test (`locust`) and p95 budgets". Queue row **left open, now precisely**: "The p95 number itself" — no `PERF_EMAIL`, and owner item 8 owns it; not run and not simulated. Queue rows **added (4, none fixed — this prompt changes no product code)**: the dashboard's card controls at **17 × 17 px** and **28 × 28 px** against the contract's ≥ 44 px; the stray guard's two blind spots (it prunes `dist`, and its pattern needs an extension, so a stray *directory* both escapes it and breaks `ng build`); **webkit's `[data-test=agree] is not reachable with the keyboard`** on the signing ceremony's consent checkbox, with what to check in real Safari to tell our markup from Safari's Tab default; and `BROWSERS=all`'s eight real failures. **Evidence**: three runs **8m23s / 8m37s / 8m27s all exit 0**, identical numbers, no flake · `@smoke` vs production **5 passed, 2 environmental, 0 real**, with `smoke-viewer` classified rather than assumed (standalone against production it draws in 3.8 s: `canvas 1056×1494, 64 distinct colours`) and `phase-8:91` now passing in-suite · p95 **6–12 ms** across all six §10.2 endpoints, 3079 requests, 0 failures, throttles restored · cross-browser **264 passed / 8 failed** after discovering that firefox and webkit had never been installed here (179 of 181 earlier "failures" were `Executable doesn't exist`)
| 9 | `handoff-to-cli-h1-production-seal-proof.md` | **GATE** queue row "Production signing certificate" (2026-08-02); `docs/ops/railway-handoff-claude-cli.md` H1; `docs/10-launch-checklist.md` "Signing" (certificate + `TSA_URL` evidence notes); Phase 8 criterion "Completed PDF opens in any validator…" on the production certificate | ✅ merged — PR #25 (`67d5e89`), 2026-08-23, archived at `docs/archived/2026-08-23-handoff-to-cli-h1-production-seal-proof.md` | `docs/h1-seal-proof` · PR #25 · `67d5e89` | PROGRESS session log **"2026-08-23 — H1 — production seal proof"**, with the raw material in `docs/reviews/evidence/h1/` (indexed by its own `README.md`). Queue row **closed**: the **GATE** row "Production signing certificate" (2026-08-02) — the certificate seals, verifies whole-document and PAdES, B-T with DigiCert's TSA, and production's own `/api/verify/` accepts the result; the environment side was **verified, not inferred** (all four signing variables set on all five Django services, `SIGNING_CERT_B64` decoding byte-identical to the local `.p12`, and a read-only probe sealing inside production's `worker-heavy`). Queue row **added**: signatures burn in 180° from upright on `/Rotate 90`/`270` pages and the envelope footer becomes a vertical ribbon that also breaks `/verify`'s envelope match (pre-existing; found by §6's by-eye pass; mechanism pinned to `signatures.py:199 _counter_rotation` and `stamp_envelope_footer`'s missing `rotate=`). Also updated: `docs/ops/railway-handoff-claude-cli.md` (H1 → done; **all three** deploy handoff items now closed), `docs/10-launch-checklist.md` "Signing" (dated evidence under **both** boxes, and the self-signed decision copied in ready to tick), `docs/ops/launch-handoff-owner.md` (H1 struck; **`TSA_URL` removed from the dashboard list** — set on all five and demonstrably working from Railway's network) |

| 10 | `handoff-to-cli-post-programme-review.md` | The 2026-08-25 post-programme review's five LOW findings, delivered as `.zen-post-programme-review.patch` (skip-guard unbound var + summed grouped skips; `adopt()` document-identity check; JSON-LD removed on destroy; compare-toast plural; scheduler comment; the PROGRESS/TRACKING/review record updates incl. the new ws-drawer queue row) | ✅ merged — PR #43 (`831766b`), 2026-08-26, archived at `docs/archived/2026-08-26-handoff-to-cli-post-programme-review.md` | `fix/post-programme-review` · PR #43 · `831766b` | PROGRESS session log **"2026-08-25 — Post-programme adversarial review"**, and in particular its **Landed (row 10), 2026-08-26** section: the gate's real numbers (backend **1164 passed / 6 skipped**, coverage apps 91.54 % / pdf_engine 91.87 %, `ng lint` clean, unit **578 / 66 files**, **43 prerendered routes** + `verify:prerender`, pg_only **6 passed**, Playwright **86 passed / 1 skipped**), run twice — once on the patch and once on the final commit — with identical results. The three browser checks are recorded there as measurements, the adopt guard **reproduced** rather than assumed. **The one e2e skip is `phase-11:164`'s `isDevServer` 404-status skip, not the webkit skip this prompt predicted** — `infra/test.sh --e2e` runs chromium only. Queue rows **added** (two, both found by the self-review, both filed rather than fixed per the prompt's own rule): the 404's `robots: noindex` outliving the 404, and "N page(s)" surviving in **18 live strings** everywhere except compare — the first of them the rotate toast seen while verifying the adopt guard. |

## Landed outside the programme (for orientation — not tracked here)

*(Renamed from "before the programme" on 2026-08-23: the first entry below landed **during** it. Work that is not one of the nine prompts still belongs somewhere findable, and this is the table for it — it does not get a numbered row, because the numbered rows are the contract for the nine.)*

| What | Where it is recorded |
|---|---|
| **Annotate text boxes** (`fix/annotate-text-fields`, PR #44 `960baba`) — the owner-reported vanishing/doubling text boxes; two root causes in the Phase 3 overlay (a cancelled `pointerdown` never blurred the editor; the raster was drawn with the annotations); Edit's scanned gate scoped to *Edit text*. | PROGRESS session log **"2026-08-26 — Filling in a form with text boxes: the field that vanished, the text that doubled"** and the archived prompt `docs/archived/2026-08-26-handoff-to-cli-annotate-text-fields.md` |
| **`apiError` follow-ups** (`fix/api-error-followups`, PR #39 `9fbff95`) — the two loose ends prompt 7 wrote into its own PR body and evidence, closed the next day. Not one of the nine, so it runs here. | PROGRESS session log **"2026-08-25 — The two things PR #38 flagged and did not fix"**. **`isApiError()` removed**: the prompt named it a deliverable and it shipped exported, tested and with no caller anywhere in the app — flagged rather than quietly left, but flagging dead code is not the same as not shipping it. The reason it never found one stays as a comment where it stood: `message` is returned verbatim and optional, so every site already spells the test `apiError(err).message ?? 'our sentence'`, and that `??` **is** the question, asked where the answer's consequence lives. **`ViewerFacade.loadVersions` gains the `error` callback `loadOutline` has always had** — and the console noise prompt 7 recorded was the smaller half: a failure of that one request while the document itself loads left the **previous** document's versions in the panel, offering "Revert to this" against version ids from a file the person had navigated away from. Both signals cleared, not just the list, because the panel would otherwise read "Showing the 0 most recent of 5"; `loadMoreVersions` handles its error by deliberately doing nothing. **One of three new cases discriminates and it was checked** — run against the unfixed facade, one goes red and two stay green, and the spec says so at the top so neither is misread as evidence. Gate: pytest **1159/6 skipped** · `ng test` **66 files / 576 tests** · Playwright **86 passed, 1 skipped** · bundle unchanged at 576.43 kB |
| **Phone follow-ups** (`fix/phone-follow-ups`) — the six items flagged when the phone workspace merged (PR #31). Four were bigger than flagged and four required correcting something written the day before, including three sentences of the design contract. Not one of the nine prompts, so it runs here. | PROGRESS session log **"2026-08-24 (later still) — The follow-ups, and what four of the six turned out to be"**, with the browser evidence in `docs/reviews/evidence/phone-follow-ups/` (indexed by its own `README.md`). Queue rows **closed**: the `dir="rtl"` ~10 000 px overflow · the fourteen `!min-h-9` fields · the annotate zoom seed (which turned out to be six panes that never fitted the page at all) · the dashboard at 390 px · Postgres's connection budget under a long test session. Queue rows **added**: five, three of them from a completeness sweep nobody asked for |
| **Rotated-page orientation** (`fix/rotated-page-orientation`) — 13 confirmed call sites across five engine modules, both rotated-page guard tests rewritten as false greens, `01-architecture.md` §8's per-API table corrected. Opened by the queue row prompt 9 filed; not owned by any prompt, so it ran on its own branch. | PROGRESS session log **"2026-08-23 (later) — Rotated pages: one wrong sign, thirteen places"**; queue row "A signature burnt onto a `/Rotate 90` or `/Rotate 270` page…" ✔ resolved; queue row **added** for the form-widget product question |
| UI audit (PRs #17, #18, `dacf623`) — cache-control middleware, viewer `.mjs`/CSP, text box, version Undo | PROGRESS session log 2026-08-20/21 |
| Follow-ups (PR #19, `df6afb9`) — pane toolbars wrap, `/organize-pdf` lands on the grid | PROGRESS session log 2026-08-21 (evening) |
| Status review revision 1 (`f8743ac`) | `docs/reviews/status-review-2026-08-21.md` |
| **Phase 12** — usability add-ons (PR #20, `ec8a33e`; record `f34800f`) | PROGRESS §Phase 12 + session log; `docs/archived/2026-08-21-phase-12-cli-handoff.md`; `docs/reviews/2026-08-21-phase-12-production-audit.md` |

## Owner-only items (not prompts)

Listed in `docs/reviews/status-review-2026-08-21.md` §5.2 and `docs/ops/launch-handoff-owner.md`:
SMTP on/off, the custom domain (Phase 11 P1), the three legal reviews (GATE), the Railway
dashboard session (`TSA_URL`, backups, `SENTRY_DSN`, recycle + restore drills), the
certificate decision, the viewer/phone/screen-reader checks, the guide skim, the `v1.0.0` tag.
Track those in `docs/10-launch-checklist.md`, which is where the owner ticks.

## How a prompt edits its row (copy this)

Start (first commit on the branch):

```
| 2 | … | … | 🔵 in progress — `fix/e2e-gate-hardening`, 2026-08-22 | `fix/e2e-gate-hardening` | — |
```

Finish (one commit on `main` after the merge, `docs(tracking): prompt 2 merged`):

```
| 2 | … | … | ✅ merged — PR #999 (`0000000`), 2026-08-22, archived at `docs/archived/2026-08-22-handoff-to-cli-e2e-gate-hardening.md` | `fix/e2e-gate-hardening` · PR #999 · `0000000` | PROGRESS session log "2026-08-22 — E2E and gate hardening"; queue rows ✔ |
```
