# PDF engine libraries — installed in api + worker image (development-plans/01-architecture.md §2)
# The ONLY place these libs are imported is backend/apps/pdf_engine/.
PyMuPDF==1.28.0
pikepdf==10.10.0
pypdf==6.14.2
ocrmypdf==17.8.1
pyHanko==0.35.2
reportlab==4.4.4
pdf2docx==0.5.8
pymupdf4llm==1.28.0
# 2.33.0 fixes PYSEC-2026-2275; we call Gotenberg and fetch URLs users
# supply, so the HTTP client is on the hostile path too.
requests==2.33.0
# Unicode bidi (UAX #9) for text boxes: the file draws RTL lines in visual
# order, the way the browser shows them (annotations._prepare_text_aps).
python-bidi==0.6.11
