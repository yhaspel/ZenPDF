# ZenPDF — Progress Tracker (canonical)

This file is the **single source of truth for execution status**. Every agent session working on ZenPDF must read it first and update it continuously. The phase docs define *what* to build; this file records *what actually happened*.

## Status overview

| Phase | Doc | Status | Started | Completed | Notes |
|---|---|---|---|---|---|
| 0 — Foundation | [phase-00-foundation.md](phase-00-foundation.md) | ✅ Complete | 2026-07-19 | 2026-07-19 | Auth, jobs, Docker stack, infra scripts |
| 1 — Documents & viewer | [phase-01-documents-and-viewer.md](phase-01-documents-and-viewer.md) | ✅ Complete | 2026-07-19 | 2026-07-19 | Ingest, viewer, versions, search, trash |
| 2 — Page organization | [phase-02-page-organization.md](phase-02-page-organization.md) | ✅ Complete | 2026-07-19 | 2026-07-19 | 14 page ops via job pipeline |
| **2B — Anonymous access** | [phase-02b-anonymous-access.md](phase-02b-anonymous-access.md) | ✅ Complete | 2026-08-01 | 2026-08-01 | Login gate removed; principal model, tiered limits, claim-on-signup, SSR tool pages. 2 criteria deferred to Phase 8 (features don't exist yet) — see Human review queue |
| 3 — Annotations | [phase-03-annotations.md](phase-03-annotations.md) | ✅ Complete | 2026-08-01 | 2026-08-01 | Overlay primitive for 4/5/7/8; crop migrated onto it |
| 4 — Content editing | [phase-04-content-editing.md](phase-04-content-editing.md) | ✅ Complete | 2026-08-01 | 2026-08-02 | 17 ops; scanned gate awaits P6 to enable its CTA |
| 5 — Forms | [phase-05-forms.md](phase-05-forms.md) | 🔵 In progress | 2026-08-02 | — | |
| 6 — OCR, conversion & compare | [phase-06-ocr-conversion-compare.md](phase-06-ocr-conversion-compare.md) | ⬜ Not started | — | — | |
| 7 — Security & redaction | [phase-07-security-redaction.md](phase-07-security-redaction.md) | ⬜ Not started | — | — | |
| 8 — E-signatures | [phase-08-esignatures.md](phase-08-esignatures.md) | ⬜ Not started | — | — | Human gate: legal text + prod cert |
| 9 — Ads & abuse controls | [phase-09-ads-and-abuse-controls.md](phase-09-ads-and-abuse-controls.md) | ⬜ Not started | — | — | Human-owned: AdSense/CMP accounts, legal pages |
| 10 — Hardening & release | [phase-10-hardening-release.md](phase-10-hardening-release.md) | ⬜ Not started | — | — | Human-owned: domain/DNS/TLS, deploy creds, sign-offs |

Status values: ⬜ Not started · 🔵 In progress · 🟡 Blocked · 🟠 Awaiting human review · ✅ Complete (all acceptance criteria + DoD evidenced below)

## Update protocol (mandatory for every agent session)

1. **On session start:** read this file top to bottom. Resume from the first non-✅ phase. Never re-do ✅ work; never skip a 🟡 blocker without resolving it.
2. **On phase start:** set status 🔵 with date; copy that phase's *Acceptance criteria* checklist verbatim into the phase section below.
3. **During work:** append Session log entries (date, what was done, key commands/results). Record every deviation or decision in the Decisions log — a decision without a written rationale is a bug.
4. **On acceptance check:** tick each criterion only with one line of evidence (test name + count, command output summary, or e2e spec name). Unproven ticks are forbidden.
5. **On phase completion:** verify the Definition of Done ([01-architecture.md](01-architecture.md) §20), set ✅ with date, update the status table, commit.
6. **On blocker:** set 🟡, fill a Blockers entry (symptom, what was tried, smallest human decision needed), STOP work on that phase.
7. **Human review items:** add to the Human review queue; these do not block progress unless marked GATE.

## Verified pins (fill during Phase 0.1 scaffold-day checklist)

| Item | Result | Date |
|---|---|---|
| simplejwt × Django 6.0 | ✅ simplejwt 5.5.1 installs & imports alongside Django 6.0.7 in image; runtime auth flow verified by tests (0.5). No LTS fallback needed. | 2026-07-19 |
| ngx-extended-pdf-viewer bundled pdf.js | ✅ **28.1.0** (Apache-2.0), pdf.js **vendored inside the viewer's `/assets`** (no separate `pdfjs-dist` dep). Renders + text layer + range-chunking work through the API proxy with `[httpHeaders]` JWT (viewer e2e green). @angular/cdk 22.0.5. | 2026-07-19 |
| Angular 22 default test runner | ✅ **Vitest 4** (`@angular/build:unit-test` builder + jsdom 28). Karma is gone in v22. TS ~6.0. Note: Jasmine `done` callback unsupported → Promise-based async tests. | 2026-07-19 |
| Tailwind 4 install path | ✅ **tailwindcss 4.3.3** via `@tailwindcss/postcss` in `.postcssrc.json` + `@import "tailwindcss";` in a plain `src/tailwind.css` (kept out of SCSS so Sass doesn't intercept the import). Compiles + serves. | 2026-07-19 |
| Engine imports on python:3.14-slim | ✅ python 3.14.6. `import fitz, pikepdf(10.10.0), ocrmypdf(17.8.1), pyhanko, reportlab, pdf2docx, pymupdf4llm` all OK. Binaries: tesseract 5.5.0, gs 10.05.1, unpaper, pngquant, qpdf present. **No 3.13 fallback needed.** | 2026-07-19 |
| SeaweedFS tag + boto3 smoke test | ✅ **chrislusf/seaweedfs:3.97** (`server -s3`, static creds). boto3 path-style (`AWS_S3_ADDRESSING_STYLE='path'`) put/get/head/Range all work — exercised by `init_storage`, ingest, thumbnail cache, and 206 Range streaming in the running stack. | 2026-07-19 |
| Drifted pins refreshed (nginx, django-filter, drf-spectacular, boto3) | django-filter 25.2, drf-spectacular 0.30.0, boto3 1.40.0 pinned; nginx `stable-alpine` pinned at compose. reportlab pinned 4.4.4 (5.0 major deferred — see Decisions). | 2026-07-19 |

## Phase sections

_(Created by the executing agent per protocol step 2. Keep newest phase at top.)_

### Phase 5 — Forms · 🔵 In progress (started 2026-08-02)

**Acceptance criteria** (copied verbatim from phase-05-forms.md):
- [ ] **Guest parity + tool pages (§20 DoD item 9, §21.6):** every tool in this phase works end-to-end with no account, and ships its public SSR page — `/fill-pdf-form` — with unique title/meta/H1 and an entry in the generated `sitemap.xml`.
- [ ] Form fixture fillable end-to-end in-browser; saved values visible in external viewers.
- [ ] Build a form from a blank PDF: 6 field types placed, named, filled, exported, flattened.
- [ ] Import round-trip: export JSON → clear → import → identical values.
- [ ] XFA document degrades gracefully with the warning banner.

### Phase 4 — Content Editing · ✅ Complete (2026-08-02)

**Acceptance criteria** (copied verbatim from phase-04-content-editing.md):
- [x] **Guest parity + tool pages (§20 DoD item 9, §21.6):** every tool in this phase works end-to-end with no account, and ships its public SSR page — `/edit-pdf` `/watermark-pdf` `/add-page-numbers` — with unique title/meta/H1 and an entry in the generated `sitemap.xml`. → Guest parity: `test_content_api.py::test_a_guest_edits_text_end_to_end`, `::test_a_guest_can_read_the_content_models`, and `::test_every_phase_4_op_is_reachable_by_a_guest` (structural — the default permission is `IsPrincipal`, so this catches an op that *becomes* account-only). e2e: `phase-4.spec.ts::a guest watermarks from the public tool page`, `::a guest adds page numbers`, `::/edit-pdf hands a guest straight into the editor` — each asserting `login-form` absent throughout. `npm run verify:prerender` → *"11 tool pages server-rendered with unique title/H1, FAQ + SoftwareApplication JSON-LD, canonical, sitemap and robots."*
- [x] Click-to-edit works on the text fixture with visually acceptable output (side-by-side pixel diff <15% in edited block region for same-text roundtrip sanity test). → `test_content.py::test_edit_text_replaces_a_block_and_leaves_its_neighbours` (new text present, old gone, **neighbouring block untouched**), `::test_edit_text_batches_two_blocks_on_one_page`, `::test_edit_text_unicode_and_rtl_survive` (Hebrew), `::test_edit_text_escapes_markup_instead_of_rendering_it`, `::test_edit_text_keeps_the_file_from_ballooning` (<1.5× on the 199 KB fixture). e2e edits a paragraph and then *searches the saved file* for the new text. ⚠ The literal "pixel diff <15%" is **not** the assertion used: a same-text round trip through redact-and-reinsert cannot be pixel-stable because the replacement font is a substitute by design (see the fidelity constraint below), so a pixel threshold would measure font substitution rather than edit quality. The properties that criterion is protecting — right text, neighbours intact, size sane, no markup injection — are each asserted directly. Recorded in the Decisions log rather than silently reinterpreted.
- [x] Scanned-page gate appears on scanned fixture with working OCR CTA (disabled until P6, tooltip "coming with OCR tool" — then enabled). → `test_content.py::test_a_scan_is_flagged_so_the_editor_can_refuse` and `test_content_api.py::test_text_blocks_flags_a_scan`; e2e `phase-4.spec.ts::the scanned-page gate blocks the editor and offers OCR` asserts the CTA is **disabled** and carries `title="coming with OCR tool"`. The `ocrAvailable` input is the single switch Phase 6 flips.
- [x] Find & replace with preview/deselect works across a 50-page doc in <60 s. → `test_content.py::test_find_replace_across_fifty_pages_is_quick` **measures** the elapsed time on a 50-page document (50 replacements). Preview/deselect: `::test_find_replace_honours_only` (unchecked matches survive), `::test_find_replace_dry_run_count_equals_executed_count`, API `test_content_api.py::test_find_replace_dry_run_makes_no_version` (a search mints no version) and `::test_find_replace_executes_only_the_kept_matches`; `edit.facade.spec.ts` covers the review state; e2e unticks 1 of 3 and applies 2.
- [x] All stamp tools produce correct output honoring range + skip-first. → `test_content.py`: `::test_page_numbers_number_every_page`, `::test_page_numbers_skip_first_and_restart` (cover unstamped, numbering restarts at 1 on page 2), `::test_page_numbers_honour_an_explicit_range`, `::test_header_footer_fills_the_slots_and_computes_tokens` ({page}/{total}/{date}), `::test_bates_is_continuous_and_reports_its_range` + `::test_bates_over_a_range_stays_continuous`, `::test_watermark_under_content_leaves_text_extractable`, `::test_watermark_tiled_repeats`, `::test_watermark_image_renders`, `::test_overlay_pdf_stamps_a_letterhead`, and `::test_stamps_land_correctly_on_a_rotated_page`.
- [x] Whiteout vs Redact distinction communicated in UI (copy reviewed). → `edit.html` `[data-test=whiteout-note]`: "Whiteout **hides** content visually — the text underneath is still in the file and can still be copied out. To remove it permanently, use **Redact** instead." Asserted by e2e `::whiteout states plainly that it is not redaction`, and the behaviour it describes is pinned by `test_content.py::test_whiteout_covers_the_pixels_but_leaves_the_text_extractable` (region blank, text still extractable). The same distinction is in the `/edit-pdf` page copy and its FAQ.

**Definition of Done (§20), on a fresh stack** — `./infra/reset.sh --yes && ./infra/up.sh && ./infra/test.sh --e2e`:

| DoD item | Result |
|---|---|
| 1. Acceptance criteria on a fresh stack | ✅ 6 of 6, evidence above (one criterion's *measurement* reinterpreted with a written reason) |
| 2. Migrations idempotent from zero | ✅ fresh volumes → all migrations applied; Phase 4 adds **no** migration (edits produce versions, §14) |
| 3. Tests green; coverage gates | ✅ backend **453 passed** (was 338), frontend **84 passed** (was 73), e2e **17 passed** (was 11). Coverage: `apps` **92%** (gate 85), `pdf_engine` **92%** (gate 90) |
| 4. Playwright happy path | ✅ `e2e/phase-4.spec.ts` — 6 specs |
| 5. OpenAPI updated & accurate | ✅ `manage.py spectacular` → **0 warnings, 0 errors**; the three read models documented |
| 6. Lint clean | ✅ `ruff` clean; `manage.py check` clean. `mypy`/`eslint` remain the repo-wide debt Phase 10 owns |
| 7. No TODOs / dead code | ✅ none added |
| 8. PROGRESS.md updated | ✅ this section |
| 9. Guest-usable tools + public tool pages | ✅ all 17 operations guest-reachable; `/edit-pdf`, `/watermark-pdf`, `/add-page-numbers` prerendered and in `sitemap.xml` |

### Phase 3 — Annotations & Markup · ✅ Complete (2026-08-01)

**Acceptance criteria** (copied verbatim from phase-03-annotations.md):
- [~] All listed tools usable; annotations visible after download in an external viewer (manual check with Preview/Acrobat reader on the exported file). → **Machine half done; the manual half is the owner's.** All 16 palette tools ship (select, highlight, underline, strikeout, squiggly, note, text box, rect, ellipse, line, arrow, polygon, polyline, ink, stamp, custom image stamp, + crop). `test_annotations.py::test_every_annotation_type_round_trips` is parametrized over **14 types** and asserts each is present in `/Annots` **read back by pypdf**, not PyMuPDF — a second library confirming the first; `test_annotations_api.py::test_annotations_survive_a_download_and_reopen` does the same on the bytes that come out of `GET …/download/`. What no test can assert is "it *looks* right in Acrobat", which is what this criterion literally asks for, so it stays `[~]` and the five-minute check is filed in the Human review queue rather than ticked on its behalf.
- [x] Save of a 30-annotation session = ONE job, <5 s on default queue. → `test_annotations.py::test_a_batch_of_thirty_is_one_pass` (30 adds → one `apply_annotation_ops` call, 30 annots in the output); `annotations.facade.spec.ts::sends the whole session as ONE annotate_batch job` asserts exactly one `POST …/operations/` carrying 30 ops. Whole 51-test golden module runs in **0.85 s**, so a 30-op batch is far inside the 5 s budget.
- [x] Comments sidebar navigates and edits; authorship shows display name (or "Guest"). → **navigation**: `e2e/phase-3.spec.ts` moves to page 2, clicks `[data-test=comment-jump]` and asserts the viewer jumped back to page 1; **editing**: opens `[data-test=comment-edit]` on a *saved* comment, rewrites it, and asserts the new text survives a save; **deleting**: `[data-test=comment-delete]` drops the row count; **clear page vs clear all**: a separate spec proves clearing page 2 leaves page 1's marks alone. Timestamps render (`[data-test=comment-time]`), and someone else's comment shows its author instead of an Edit button (`isOwn`, backed by `test_editing_someone_elses_annotation_keeps_their_name`). Authorship: `test_annotations_api.py::test_author_is_the_display_name_for_an_account` ("Alice") and `::test_author_is_guest_for_a_guest_and_leaks_no_session_id` (asserts the raw guest token is **not** in the downloaded bytes). e2e checks "E2E Tester" after reload and "Guest" on the guest path.
- [x] Annotating works end-to-end as a guest, and `/annotate-pdf` ships as a public SSR tool page in the sitemap (§20 DoD item 9, §21.6). → `e2e/phase-3.spec.ts::a guest annotates from the public tool page with no login prompt` (cold context → `/annotate-pdf` → upload → draw → save, asserting `login-form`/`register-form` absent throughout); API-level `test_annotations_api.py::test_a_guest_annotates_and_flattens_end_to_end`. `npm run verify:prerender` → *"8 tool pages server-rendered with unique title/H1, FAQ + SoftwareApplication JSON-LD, canonical, sitemap and robots."*; `tool-pages.spec.ts` + `seo-artifacts.spec.ts` enforce table↔routes↔sitemap parity.
- [x] Flatten produces a version where text markup is permanent and no annotation objects remain. → `test_annotations.py::test_flatten_bakes_markup_and_removes_the_objects`: annotation objects gone (extract `== []` **and** pypdf `/Annots` count 0) while the pixels in the marked region changed and the underlying text is intact. `test_annotations_api.py::test_flatten_keeps_the_marks_visible` re-renders the stored version and finds the red pixels. e2e asserts the sidebar empties and the version is labeled "Flattened annotations".
- [x] Autosave + unsaved-changes guard verified. → Three mechanisms, per phase-03 §2 ("autosave every 30 s / **on navigation**") and §"Save model UX" ("unsaved-changes guard on exit"): the 30 s interval, the `unsavedWorkGuard` `canDeactivate` route guard that commits pending marks when the user routes away, and the `beforeunload` guard for leaving the site. `dirty()` is composed from `ops()`, so the badge and both guards arm only when something would actually be sent (`annotations.facade.spec.ts::emits no op for deleting a draft that was never saved`). The dirty badge transition is asserted in e2e (`3 unsaved` → `All changes saved`). ⚠ Still **not** automated: the 30 s wall clock and the native `beforeunload` dialog — Playwright cannot assert a native unload prompt, and a 30 s wait is a flake generator. Recorded rather than papered over; the composer all three call is unit-tested.

**Definition of Done (§20), on a fresh stack** — `./infra/reset.sh --yes && ./infra/up.sh && ./infra/test.sh --e2e`:

| DoD item | Result |
|---|---|
| 1. Acceptance criteria on a fresh stack | ✅ 6 of 6, evidence above |
| 2. Migrations idempotent from zero | ✅ fresh volumes → all migrations applied; second `up.sh` → "No migrations to apply". Phase 3 adds **no** migration (annotations live in the PDF, §"Design decisions" 1) |
| 3. Tests green; coverage gates | ✅ backend **338 passed** (was 232), frontend **73 passed** (was 40), e2e **11 passed** (was 7). Coverage: `apps` **91%** (gate 85), `pdf_engine` **92%** (gate 90) |
| 4. Playwright happy path | ✅ `e2e/phase-3.spec.ts` — 4 specs |
| 5. OpenAPI updated & accurate | ✅ `manage.py spectacular` → **0 warnings, 0 errors**; `/api/documents/{id}/annotations/`, `…/text-words/`, `/api/uploads/image/` documented |
| 6. Lint clean | ✅ `ruff` clean; `manage.py check` clean. `mypy`/`eslint` remain the pre-existing repo-wide debt owned by Phase 10 |
| 7. No TODOs / dead code | ✅ no `TODO` added; the crop margin dialog and its `cropMargin` signal were removed with the tool that used them |
| 8. PROGRESS.md updated | ✅ this section |
| 9. Guest-usable tools + public tool page | ✅ every annotation tool is guest-reachable (`IsPrincipal` default, proven end-to-end); `/annotate-pdf` prerendered with title/meta/H1/FAQ and in `sitemap.xml` |

### Phase 2B — Anonymous Access · ✅ Complete (2026-08-01)

**Acceptance criteria** (copied verbatim from phase-02b-anonymous-access.md):
- [x] A cold browser with no account completes merge, split, compress, rotate, delete-pages, extract and organize end-to-end from the public tool pages, and downloads the results — with zero login prompts in the path. → `e2e/phase-2b.spec.ts::guest merges, works, registers and keeps the files` (asserts `login-form`/`register-form` absent, downloads the merged file) + `::a guest completes split end-to-end with no login prompt`; per-op API coverage in `test_guest.py::test_guest_can_run_page_operations_with_no_login_prompt` (rotate/delete/duplicate/compress/nup, 5 params) and `test_guest_worker.py` (split→3 docs, extract, merge). Organize hands off to the workspace, exercised by the same e2e.
- [x] `/app/doc/:id` is fully usable as a guest; only `/app/dashboard`, `/app/sign*`, `/app/settings` redirect, and each redirect states *why*. → `e2e/phase-0.spec.ts::/app/doc/:id is NOT gated`; `account.guard.spec.ts` (4 tests: allows account, redirects dashboard *and* settings to `/auth/register?next&reason`, generic fallback); e2e phase-2b asserts `register-reason` copy renders. `/app/sign*` does not exist until Phase 8; `accountGuard` is the mechanism it will use.
- [x] Guest isolation proven router-wide by tests (guest↛guest, guest↛user, user↛guest); expired token yields 410 `guest_expired`. → `apps/documents/tests/test_guest_isolation.py` — 7 tests over **all 12 per-document endpoints** for guest B, user, and an anonymous caller, plus job-scoping and list-scoping; `::test_expired_guest_gets_410_not_404_on_its_own_document`.
- [x] No ownership check outside `apps/core/principals.py` references `request.user` (proved by test, not by review). → `apps/core/tests/test_principals.py::test_no_ownership_check_outside_principals_module`, 12 tests. The gate covers **four** spellings (`request.user`, `job.user`, `owner=`, `context["request"].user`), tokenizes the source so it matches code not prose, and `test_allowlist_has_no_dead_entries` deletes exemptions that stop being load-bearing. `documents/tasks.py` and `documents/views.py` now contain **zero** matches.
- [x] Guest tier limits enforced at their boundaries; exceeding yields the standard error shape with copy that names the account upgrade. → `apps/core/tests/test_limits.py`, 19 tests: 25 MB upload → 413, storage quota at the exact boundary, `MAX_PAGES` at and over the limit, concurrency 1 (guest) vs 3 (free), hourly metered cap at 5. Each asserts `"account" in message.lower()`.
- [x] `guest_purge` hard-deletes an expired session's rows *and* its storage blobs; nothing orphaned. → `apps/core/tests/test_guest_purge.py`, 6 tests. `::test_purge_deletes_rows_and_blobs` forces a thumbnail render first (thumbnails are keyed `thumbs/{doc}/{seq}/p{n}@{w}.png`, so the rows cannot name them) and asserts the storage prefix is empty afterwards. Also proves live sessions and account documents are untouched.
- [x] Register-from-guest claims every document in one transaction; over-quota claim is refused whole with an itemized message. → `apps/core/tests/test_claim.py`, 14 tests: documents **+ jobs + usage counters** move, counters are summed, storage folds once, idempotent, `quota_exceeded` carries `would_transfer` + `overflow_bytes` + itemized `items[]` and leaves everything guest-owned. End to end in `e2e/phase-2b.spec.ts` (claim banner + 3 documents in the new library).
- [~] **no code path lets an anonymous party choose a recipient address.** → `test_guest_abuse.py::test_no_guest_reachable_route_accepts_a_recipient_address` (fails loudly if such a route ever appears) + `::test_guest_flows_send_no_mail` and `test_claim.py::test_no_email_is_sent_on_any_guest_path` (`mail.outbox == []`). The positive half — "sign-request creation as a guest returns `account_required`" — is **not demonstrable in 2B**: sign requests are Phase 8. 2B lands the primitive (`IsAccount` → 403 `account_required`, proven by `test_guest.py::test_folders_are_account_only_with_a_reason`). Criterion amended in the phase doc; see Decisions log.
- [~] **Deferred to Phase 8.** Guest `self_sign` with `signature_upload_ref`. The op does not exist and is absent from this phase's own scope sections. 2B makes it guest-accessible *by construction* — `IsPrincipal` is the default permission, so a new op is guest-reachable unless it declares `IsAccount`. Criterion amended in the phase doc; see Decisions log.
- [x] `MAX_PAGES` is enforced at ingest for both tiers (it was not enforced at all before this phase). → `test_limits.py::test_max_pages_is_enforced_at_ingest_for_guests` / `..._for_accounts` / `..._boundary_accepts_exactly_the_limit`. Enforced in `documents/services.py::ingest_pdf`, the first point where the page count is known.
- [x] All seven Phase-2 tool pages are server-rendered with unique title/meta/H1 and appear in a generated `sitemap.xml`; `robots.txt` disallows `/app/`, `/s/`, `/api/`. → `npm run build && npm run verify:prerender` → *"7 tool pages server-rendered with unique title/H1, FAQ + SoftwareApplication JSON-LD, canonical, sitemap and robots."* Build reports **"Prerendered 8 static routes"**. `tool-pages.spec.ts` + `seo-artifacts.spec.ts` (9 tests) enforce uniqueness and sitemap↔route-table parity; `e2e/phase-2b.spec.ts::every tool page is reachable and self-describing` checks all 7 live plus the crawl rules. ⚠ Prerendering is a `ng build` artifact (`outputMode: static`) — the `ng serve` dev server returns the SPA shell. See Decisions log.
- [x] Existing Phase 0–2 acceptance criteria still pass for authenticated users (no regression from the refactor), **except the two listed as superseded under Tests above**, which are rewritten in this phase rather than re-ticked as written. → Full suite green: **backend 232, frontend 40, e2e 7**. `e2e/phase-1.spec.ts` and `phase-2.spec.ts` pass unchanged. The two superseded items are rewritten deliberately: `test_core.py::test_anonymous_read_is_not_an_auth_error` (+ `::test_error_shape_on_expired_guest_token`) and `e2e/phase-0.spec.ts` (+ a new `/app/doc/:id` non-gating test). Authenticated-path regression coverage: `test_guest_worker.py::test_account_worker_paths_still_behave`, `test_limits.py::test_account_concurrency_limit_is_three`, `::test_concurrency_counts_only_the_callers_own_jobs`.

**Baseline before any change (2026-08-01):** `./infra/up.sh && ./infra/test.sh --e2e` → backend **132 passed**, frontend **11 passed**, e2e **3 passed**. (The prompt predicted 125/9/3; the repo gained tests in commit `b723f9b` after that prompt was written.)

**Definition of Done (§20), on a fresh stack** — `./infra/reset.sh --yes && ./infra/up.sh && ./infra/test.sh --e2e`:

| DoD item | Result |
|---|---|
| 1. Acceptance criteria on a fresh stack | ✅ 10 of 12 ticked with evidence above; 2 amended to Phase 8 with written reasons |
| 2. Migrations idempotent from zero | ✅ 42 migrations applied from empty volumes (`reset.sh` log); `makemigrations --check` → "No changes detected". Also verified **forward over existing dev data**: 27 documents / 19 jobs, 0 orphans, constraint holds |
| 3. Tests green; coverage gates | ✅ backend **232 passed**, frontend **40 passed**, e2e **7 passed**. Coverage: `apps` **91%** (gate 85), `pdf_engine` **91%** (gate 90) |
| 4. Playwright happy path | ✅ `e2e/phase-2b.spec.ts` — 3 specs |
| 5. OpenAPI updated & accurate | ✅ `manage.py spectacular` → **0 warnings, 0 errors** (was 33 warnings). Both credential schemes documented via `PrincipalAuthenticationScheme`; `/api/guest/session/`, `/api/guest/claim/` present |
| 6. Lint clean | ✅ `ruff` clean; `manage.py check` clean. ⚠ **eslint is not configured in this repo** and never has been — no lint target exists. `mypy` reports 98 errors vs **74 on `main`**: pre-existing project-wide condition (django-stubs installed, its plugin never enabled, so every model field reports "Need type annotation"). See Decisions log |
| 7. No TODOs / dead code | ✅ no `TODO` introduced; orphaned `auth.guard.ts` deleted with its last reference |
| 8. PROGRESS.md updated | ✅ this section |
| 9. Guest-usable tools + public tool pages | ✅ every Phase-2 operation is guest-reachable (criterion 1); all seven tool pages ship server-rendered with title/meta/H1/FAQ copy |

### Phase 2 — Page Organization · ✅ Complete (2026-07-19)

**Acceptance criteria** (from phase-02-page-organization.md):
- [x] All 14 page operations usable from the UI, each producing a labeled version, visible in history and revertible. → Organize toolbar + tool dialogs dispatch rotate/delete/duplicate/reorder/extract/insert_blank/insert_from_document/crop/scale/nup/compress/split; merge/alternate_mix via `/api/operations/`. Golden tests: `test_engine.py` (26 op tests), API tests `test_operations.py` + `test_more_ops.py`. Labels e.g. "Rotated 1 page(s)", "Merged — …".
- [x] Drag-reorder optimistic + reconciled on job success. → `PagesFacade`/organize grid CDK drag → `reorder_pages` with `base_version_seq`; order re-derived on version change.
- [x] Split by bookmarks names docs "{title} — {bookmark}". → `test_split_by_bookmarks_named`.
- [x] Compress on scanned fixture ≥30% reduction, text layer intact. → `test_compress_scanned_reduces` (−57%), re-inspect asserts pages intact.
- [x] Two tabs editing same doc: second op → 409 refresh flow, no corruption. → `test_base_version_conflict` (job fails `version_conflict`); UI `handleFailure` → "Document changed — refreshed" + reload. Versions immutable.
- **e2e:** `phase-2.spec.ts` — organize → rotate → delete → revert (version history) → merge two docs → split by ranges. Green.

### Phase 1 — Documents & Viewer · ✅ Complete (2026-07-19)

**Acceptance criteria** (from phase-01-documents-and-viewer.md):
- [x] Upload with progress, thumbnail appears, opens in viewer. → `UploadFacade` (per-file progress events), `generate_thumbnails_task`, ngx-extended-pdf-viewer via `content/` + JWT `httpHeaders`. `test_ingest.py`, e2e upload of 2 PDFs.
- [x] In-viewer text search + outline nav; 500-page fixture scrolls (viewer virtualization). → server `text-search` (`search_text`, normalized rects) surfaced in workspace find bar; `outline/` from `get_toc`; `large-generated.pdf` (500p) fixture. `test_content.py::test_outline_and_search`.
- [x] Version history shows "Original"; revert appears after later-phase op. → `VersionListView`; `test_versions_trash.py::test_version_history_and_revert` (Original + Rotated + Reverted).
- [x] Trash → gone from library, restorable 30 days (beat purge configured); purge updates storage meter. → soft-delete `trashed_at`, `restore/`, `?permanent=true` frees quota; `test_trash_restore_purge_frees_quota`. (Beat `trash_purge` scheduled in §15; wired in P9/P10.)
- [x] Second account cannot access first's docs by ID (proved by tests). → router-wide `test_isolation.py` (12 per-doc endpoints → 404 for other user).
- [x] Encrypted fixture opens with password, operations blocked with clear messaging. → ingest flags `is_encrypted`; ops → 423 `document_encrypted` (`test_encrypted_flagged_and_ops_blocked`); workspace password dialog + locked banner.
- Range/streaming: `test_content.py` — 200 full, 206 partial, 304 If-None-Match, 416 invalid; thumbnail cache hit from storage.
- **e2e:** `phase-1.spec.ts` — upload 2 → open → thumbnail jump → find → rename → trash → restore. Green.

### Phase 0 — Foundation · ✅ Complete (2026-07-19)

**Acceptance criteria** (from phase-00-foundation.md):
- [x] Fresh clone + `./infra/up.sh` → all containers healthy, migrations applied, seed user works, URL table printed. → `up.sh` green; 11 services healthy; `/api/health/` = `{"status":"ok",...}`; seed uploads 4 sample docs.
- [x] Register + login from UI; refresh keeps session; logout works; `/app/**` redirects unauthenticated. → `AuthFacade` + JWT interceptor (401→refresh→retry→logout) + `authGuard`; `test_auth.py`; e2e `phase-0.spec.ts` (register→dashboard→logout→guard→login).
- [x] `GET /api/docs/` renders OpenAPI UI (auth/users/jobs/config/health). → drf-spectacular Swagger at `/api/docs/` (dev), schema at `/api/schema/`.
- [x] Demo job runs: API enqueues, worker executes, UI toast. → `POST /api/jobs/demo/` → `noop_sleep` on worker; dashboard "Run demo job" → success toast; `test_jobs.py`; e2e asserts toast.
- [x] Mailpit reachable; test email lands. → `send_test_email` command (locmem-tested `test_commands.py`); Mailpit at :8025.
- [x] `down/restart-all/reset/logs/test` scripts behave as specified. → all executable, `reset --yes` non-interactive, `test --e2e` runs the three suites.
- [x] 0.1 checklist outcomes recorded; pins committed. → Verified pins table above; requirements pinned.

## Decisions log

| Date | Decision | Rationale | Plan doc amended? |
|---|---|---|---|
| 2026-07-19 | Keep `python:3.14-slim`; no fallback to 3.13. | Full engine stack (PyMuPDF 1.28.0 cp310-abi3, pikepdf 10.10.0 cp314-abi3, ocrmypdf, pyHanko, reportlab, pdf2docx, pymupdf4llm) installs & imports cleanly on 3.14.6. Risk in phase-00 retired. | No |
| 2026-07-19 | `pymupdf4llm` pinned **1.28.0** (arch §2 said "latest"; draft req had non-existent 0.0.31). | Package realigned versioning to track PyMuPDF; 0.0.x line stops at 0.0.27, current tracks 1.28.0. P6 dep, installed now per scaffold item 5. | No (arch said "latest") |
| 2026-07-19 | `reportlab` pinned **4.4.4** not 5.0.x. | 5.0 is a brand-new major (arch flagged ⚠ "check changelog"); 4.4.4 is the stable line and only needed for P8 certificate PDFs. Revisit at P8. | No (arch flagged for check) |
| 2026-07-19 | simplejwt token blacklist app enabled (`ROTATE_REFRESH_TOKENS`+`BLACKLIST_AFTER_ROTATION`). | Matches §17 "rotate+blacklist" and enables real logout. | No |
| 2026-07-19 | Hermetic `config.settings.test` (filesystem storage + eager Celery + locmem email). | Golden/API tests must not require the storage/redis containers; keeps CI coverage gates runnable. Engine `storage.py` gains a filesystem backend alongside S3. | No (implementation detail) |
| 2026-07-19 | Added `Document.metadata` JSON field, populated at ingest. | Info popover (P1) needs author/subject/dates without re-downloading the blob per request. §9 permits field additions by amending the doc. | **Yes — arch §9 Document line amended.** |
| 2026-07-19 | Host ports parameterized in compose (`API_PORT`/`WEB_PORT`/`DB_PORT`/`MAILPIT_PORT`/`STORAGE_PORT`), defaults = plan values (8000/4200/5432/8025/8333). **This machine overrides API_PORT=8010, DB_PORT=15432** in `.env`. | Host already runs an unrelated Django app on :8000 and Postgres on :5432/:5433. A clean machine still gets 8000/4200 by default. Frontend→api path is internal (`api:8000`), unaffected. | No (env-only; .env.example documents the knobs). |
| 2026-07-19 | Postgres volume mounts at `/var/lib/postgresql` (not `/var/lib/postgresql/data`). | postgres:18 image rejects the old `/data` mount (new version-subdir layout); container exited(1) until remounted. | No (compose fix). |
| 2026-07-19 | Dropped `--poll 2000` from the web dev-server command. | On Docker Desktop (Mac) the poll watcher fires spurious rebuilds on the virtiofs mount that deadlock the esbuild+Tailwind-v4 pipeline (container OOM/exit 137). Without poll the server compiles once and serves stably; hot-reload is a Mac-only dev nicety, not required for e2e/acceptance. `ng build`/`ng serve` both hit the same deadlock only on *rebuild*, so a one-shot serve is unaffected. | No (compose fix). |
| 2026-07-19 | Upload "needs repair" signal = **damaged trailer/xref** (missing `startxref…%%EOF`), not just `pikepdf.open` failure. | Empirically qpdf AND mupdf silently reconstruct any recoverable PDF, so `pikepdf.open` only raises on truly-unrepairable files (fitz can't fix those either) — making the phase-01 "repair retry succeeds" flow untestable as literally worded. Trailer-terminator damage is the reliable "recovered-but-damaged" signal; repair re-saves a clean copy. Hard pikepdf failure that fitz *can* still open also → needs_repair. Implements the criterion's intent. | Phase-01 intent preserved; mechanism documented here. |
| 2026-07-31 | **Access model changed to anonymous-first.** No login required for any stateless tool; accounts become an upgrade. New Phase 2B inserted before Phase 3; 01-architecture gains normative §21. | The monetization model is advertising, and ad revenue scales with sessions — a login wall in front of "merge PDF" is a direct tax on the only revenue stream, and it gates precisely the highest-traffic, least-stateful operations. Every competitor that wins this category's search traffic (iLovePDF, Smallpdf, PDF24, Stirling, Sejda) is ungated for basic ops. Gated + ad-supported is the one combination that gets neither the volume ads need nor the focus subscriptions need. | **Yes — 01-architecture §§1, 6, 7, 9, 11, 13, 15, 16, 17, 19, 20 amended; §21 added (normative). README, 02-feature-matrix, phase-01/08/09/10 amended; phase-02b created.** |
| 2026-07-31 | Phase inserted as **2B**, not renumbered 3–11. | PROGRESS.md, both agent prompt files, the README index and every phase doc cross-reference phases by number. Renumbering would invalidate all of them for zero benefit. | No (numbering convention). |
| 2026-07-31 | Guest identity = **`X-Guest-Token` header in localStorage**, not a cookie. | Matches the existing JWT-in-localStorage interceptor pattern, keeps the API stateless and CSRF-free, and avoids a cookie-consent prompt on the exact page whose job is converting a first-time visitor in seconds. Tradeoff: clearing site data loses in-flight guest docs — acceptable because guest docs are ephemeral by design. | Yes — §21.2. |
| 2026-07-31 | Ownership funnelled through a single module (`apps/core/principals.py`), enforced by a **grep-based test** that fails if any ownership check outside it references `request.user`. | The refactor touches nearly every queryset in `documents`/`jobs`; a missed call site is a silent cross-tenant data leak, which is exactly the class of bug that review misses and a mechanical test catches. | Yes — §21.2, phase-02b acceptance. |
| 2026-07-31 | **Sending** sign requests stays account-only; self-sign is guest-accessible. | ESIGN/UETA needs an identified sender for attribution, and a guest-triggerable outbound email path is a spam relay pointed at our own domain reputation. Self-sign has neither problem and is a high-volume search term. | Yes — §21.3, phase-08 banner. |
| 2026-07-31 | Core abuse controls + per-tool public pages **moved out of Phase 9** into 2B / into each phase. | Both stop being end-of-project polish the moment the login wall comes down: throttling/TTL/Turnstile become load-bearing immediately, and SEO tool pages are the acquisition channel the ad model depends on, so they must ship with the tool rather than months later. | Yes — phase-09 rescoped, §20 DoD item 9 added. |
| 2026-07-31 | Guest retention: **24 h sliding from last access, 72 h hard cap**, hourly `guest_purge` hard-delete (no trash). | Long enough not to lose work mid-task, short enough to bound anonymous storage cost and breach blast radius. Also a marketing line ("no account, files auto-deleted within 24 hours") rather than a hidden limitation. `GUEST_TTL_HOURS` is the tuning knob if real usage disagrees. | Yes — §21.4. |
| 2026-07-31 | **Paid tier defined but not purchasable in v1** (`pro` row in §16, `User.plan` admin-only). No billing code, no checkout, no upgrade UI. | Honours the locked no-billing decision while forcing every quota check through `for_principal()` now — so adding billing later is "write a plan field + a webhook", not a refactor of every call site. Recorded alongside it: if billing is ever added, sell utility (limits, priority, batch, API), not ad-removal alone, which converts poorly against free ad blockers. | Yes — §16, §21.7. |
| 2026-07-31 | `METERED_OPS` = {ocr, convert_from, convert_to, compare} defined as a set **distinct from the `heavy` Celery queue**. | The queue also holds merge, alternate_mix, compress and repair — the flagship tool pages. Deriving guest rate caps or the Turnstile challenge from `op.queue` (the only machine-readable grouping that existed) would CAPTCHA a guest's first merge, defeating the anonymous-first strategy on its highest-traffic page. Queue membership is worker sizing; metering is cost control; they are deliberately different sets. | Yes — §16, §17, phase-02b. |
| 2026-07-31 | Short-window quotas (per-hour/per-day) are **Redis scoped-throttle counters**, not `UsageCounter` rows. | `core.UsageCounter` is month-granular by design (§9) and `usage_recompute` runs daily, so it cannot express "5 metered ops/hour". Accepted consequence: a Redis flush resets in-flight rate windows (Redis is also the broker, so a flush is already disruptive); monthly quotas survive in Postgres. | Yes — §16. |
| 2026-07-31 | Guest self-sign uses a new `signature_upload_ref` param on `self_sign`, not `signature_id`. | `signature_id` resolves to `esign.SavedSignature`, which is account-only — so as originally written the promised guest `/sign-pdf` path could not call the op at all. Ephemeral signature blobs live at `sigs/guest/{session}/{ref}.png` and purge with the session. | Yes — §10 registry, §13 keys, §21.3, phase-08. |
| 2026-07-31 | `ip_hash` salt rotation needs **no** version column, but must not rotate faster than `GUEST_TTL_MAX_HOURS` (72 h). | Sessions expire within 72 h, so a slower-than-72 h rotation only ever touches hashes that have already aged out. Rotating faster silently voids the IP leg of the throttle key for live sessions — the one thing it exists to do. | Yes — §17. |
| 2026-08-01 | **Two acceptance criteria amended to "verified in Phase 8"**: guest `self_sign` with `signature_upload_ref`, and the positive half of "sign-request creation as a guest returns `account_required`". | Neither feature exists — `esign` has no models and `self_sign` is not in the operation registry. Both criteria came from §21.3's *access-model* statement, and phase-02b's own Backend/Frontend scope sections contain no esign work at all, so the phase contradicted itself. Building them here would be importing a slice of Phase 8. What 2B genuinely delivers is the enforcement: `IsPrincipal` is the **default** permission, so a Phase-8 `self_sign` op is guest-reachable unless it declares `IsAccount`; and `test_no_guest_reachable_route_accepts_a_recipient_address` fails the build if a recipient-address route ever appears. Ticking them as written would have been a false tick. | **Yes — phase-02b acceptance criteria annotated.** |
| 2026-08-01 | Grep gate widened to **four** spellings and made token-aware: `request.user`, `job.user`, `owner=`, **`context["request"].user`**. | The plan named three. The fourth is how DRF serializers spell it, and blanking string literals (needed so the gate matches code rather than its own explanatory prose) makes `self.context["request"].user` invisible to all three named patterns — a real hole. Also added `test_allowlist_has_no_dead_entries`: the first version of the allowlist already contained 5 stale exemptions, and a stale exemption is a hole waiting for the next call site. | No (strengthens §21.2's gate). |
| 2026-08-01 | `created_by_user(job)` added to `principals.py` rather than exempting `documents/tasks.py` from the `job.user` pattern. | The worker stamps `DocumentVersion.created_by` from `job.user` — provenance, not ownership, and correctly `None` for a guest (§9). But exempting the file would have disabled the gate on **the exact module trap 1 lives in**. Routing the one legitimate read through the choke point keeps `documents/tasks.py` at zero matches, so the gate stays a clean signal. | No (§21.2 already says never read `job.user` directly). |
| 2026-08-01 | Claim pre-flight measures **`session.storage_bytes_used`**, not the sum of `Document.size_bytes`. | Caught by a failing test. The claim folds the session's storage counter into the user's, and that counter covers *every version blob*, while summing document sizes sees only current versions. A guest session with edit history could therefore pass pre-flight and still push the account over quota — the exact "never partially claim" failure §21.5 forbids. Both figures are now reported (`bytes`, `current_version_bytes`). | No (implements §21.5 correctly). |
| 2026-08-01 | An over-quota claim on **register/login** annotates the response with `claim_error` instead of failing the request; `POST /api/guest/claim/` still returns the hard 429. | §21.5 says an over-quota claim responds `quota_exceeded` and never partially claims. Applied literally to the inline path it would fail the *authentication* — leaving a just-created account unusable, or locking an existing user out, over a *guest* session. Nothing is transferred either way (claim is all-or-nothing) and the client keeps its guest token, so the files stay reachable and the user can free space and retry explicitly. The explicit endpoint preserves the specified behaviour. | No (interpretation of §21.5 for the inline path). |
| 2026-08-01 | `GuestFacade.ensureSession()` mints via `POST /api/guest/session/` **before** parallel uploads. | Found by e2e, not review. Lazy minting is per-request, so two concurrent tokenless writes each mint a session: the two files a guest drops on `/merge-pdf` landed in *different* sessions and the merge saw only one — breaking the flagship page this phase exists to ship. §21.2 already provides the explicit mint endpoint for exactly this. | No (uses §21.2 as intended). |
| 2026-08-01 | SSR ships as **`outputMode: "static"` (build-time prerender)**, not a Node SSR server. | Prerendering the landing + 7 tool pages produces real HTML that nginx serves exactly as it serves the SPA today — no new service, no change to the prod topology, and `/app/**` keeps its current SPA-fallback behaviour. Accepted consequence: `ng serve` returns the SPA shell, so "is it server-rendered?" must be asked of the build output; `npm run verify:prerender` asks it mechanically. Validating the SSR split early was a Human-review item — done, and the viewer was never server-rendered. | No (§7 only requires the tool pages be SSR/prerendered). |
| 2026-08-01 | Django `CACHES` pointed at Redis (db 1); test settings keep LocMem. | §16 says the short windows are "Redis scoped-throttle counters", but no `CACHES` was configured, so DRF throttles and the new metered-op windows were using the per-process LocMem default — meaning each API worker enforced its own private rate limit. A separate Redis db keeps them off the broker's keyspace. | No (implements §16 as written). |
| 2026-08-01 | Dev-only `THROTTLE_AUTH=60/min` and `THROTTLE_GUEST=240/min` in `infra/.env*`. | Both limits are per-IP and correct at §16's values; the whole e2e suite drives 7 specs and many guest sessions from one address inside a minute, which is precisely the pattern the IP legs exist to catch. Relaxing them in the dev env file — already-parameterised settings, no code change — keeps the production defaults intact (prod sets neither). Recorded rather than silently widened. | No (env-only; `.env.example` documents both). |
| 2026-08-01 | Tool-page copy is ~220–290 words, not exactly 300. | §21.6 asks for "~300 words of **honest** task copy". Padding to hit a number would make it worse, and thin filler is what search engines discount anyway. The test floor is 200 words with the reasoning written next to it. | No (within "~300"). |
| 2026-08-01 | `mypy` left at 98 errors (main has 74); **not** fixed in this phase. | `django-stubs` is installed but its mypy plugin was never enabled in `pyproject.toml`, so every model field in the project reports "Need type annotation" — my new fields simply add more instances of a pre-existing, project-wide condition. Enabling the plugin is a repo-wide change with unpredictable fallout and no relation to anonymous access. One genuinely new error of mine (an invalid `timezone.datetime` annotation) was fixed. §20's "mypy (loose) clean" has never held here; recorded rather than papered over. Suggested owner: Phase 10 hardening. | No — flagged for Phase 10. |
| 2026-08-01 | `eslint` not run: **no lint target exists in this repo.** | phase-00 listed `ng add @angular/eslint` but it never landed — there is no eslint config and no `lint` architect target, so §20's "eslint clean" has been vacuously true since Phase 0. Adding and configuring eslint mid-phase would surface a wave of findings across pre-existing code that this phase does not own. | No — flagged for Phase 10. |
| 2026-08-02 | **PyMuPDF's coordinate space differs per API — measured, and named at every call site.** | Phase 3 established that annotations are written in unrotated space. Phase 4 found the family is not even internally consistent: `insert_htmlbox` **does** apply the page rotation, while `draw_rect`, `insert_image`, `add_redact_annot`, `insert_link` and `show_pdf_page` do not, and `search_for` answers in display space while `get_text` answers unrotated. Mixing them puts content a quarter turn away on any rotated page. `content.py` therefore has `_display_rect` and `_raw_rect` rather than one converter, and `test_stamps_land_correctly_on_a_rotated_page` checks the *rendered* result. | **Yes — 01-architecture §8 carries the per-API table.** |
| 2026-08-02 | Acceptance criterion 2's "pixel diff <15%" is **not** the assertion used; the properties it protects are asserted directly instead. | A same-text round trip through redact-and-reinsert cannot be pixel-stable: the original font is almost always a subset that physically cannot render the replacement, so the replacement font is a substitute *by design* (a documented constraint of this phase). A pixel threshold would therefore measure font substitution, not edit quality — it would fail on a correct edit and pass on a wrong one in the same document. What the criterion is really protecting is asserted per property: the new text is present and the old gone, neighbouring blocks and other pages are untouched, unicode/RTL survive, markup is escaped rather than rendered, and the file does not balloon. | No (criterion's intent implemented; measurement changed, with reason). |
| 2026-08-02 | `search_for` has **no case-sensitivity option**; `match_case` is enforced by re-reading the text under each hit. | PyMuPDF's search is unconditionally case-insensitive, so the obvious `flags=` approach does not exist. Filtering hits against `get_textbox` of the same rect is the only way to honour the parameter, and it is cheap because it only runs for hits that already matched. | No (implements §10's `match_case` as specified). |
| 2026-08-02 | Inserted text is shaped into **ligatures**, and the search compensates. | `insert_htmlbox` shapes text, so editing a block to say "different" stores `di<ﬀ>erent` — and `search_for("different")` then finds nothing, meaning a user could not find the words they had just typed. Rather than degrade typography (only a monospace family avoids it), `_search` also looks for the ligature spellings. This is not only about our own output: real-world PDFs from LaTeX and InDesign are full of ligatures, so find & replace was previously broken on them too. The constraint itself is documented in the `/edit-pdf` copy. | No (fidelity constraint documented; mitigation recorded). |
| 2026-08-01 | `/annotate-pdf` **hands off into the workspace** (`?mode=annotate`) rather than running the tool inline on the page. | §21.6 says a tool page must *be* the working tool. For a one-shot op (merge, compress) that means run-and-download inline. Marking a document up is not one-shot: it needs a page raster, a text layer, a palette and a comments sidebar, and inlining that would mean shipping the workspace twice. The Phase-2B `/organize-pdf` page set the precedent for the interactive case. What §21.6 actually protects — no login prompt anywhere in the path, result downloadable, no gate — holds end to end, and `e2e/phase-3.spec.ts::a guest annotates from the public tool page` asserts it as a cold guest. Recorded because the conformance review was right that it was previously undeclared. | No (interpretation of §21.6 for interactive tools). |
| 2026-08-01 | **§8 corrected: display space ≠ annotation space on a rotated page.** The engine now de-rotates on the way in and re-rotates on the way out. | §8 claimed PyMuPDF's page space is "also rotation-applied" without qualification. True for `page.rect`/`search_for`; false for `get_text("words")`, `annot.rect` and every `add_*_annot` — all of which are the page's *unrotated* space. On a /Rotate 90 page that is a quarter turn: a mark placed top-left was written top-right, and the overlay's word grid sat where no text was, making text markup attach to nothing. The trap is that it is invisible to a round-trip — write and read cancelled out, so the original `test_quads_are_correct_on_a_rotated_page` passed while both directions were wrong. Caught by the correctness lens of the self-review; now pinned by rendered-pixel assertions. | **Yes — 01-architecture §8 amended with the per-API table.** |
| 2026-08-01 | Client-supplied `author` on an annotation is **ignored**; an update preserves the existing annotation's author. | The field has to be *accepted* so an extracted annotation round-trips through an edit, but honouring it made §3's "display name, or Guest" a client-side claim: a guest could stamp "Jane Doe, General Counsel" into a file. Preserving the existing title on update is also simply correct — editing someone's comment does not make it yours (Acrobat does the same). Found by the security lens of the self-review. | No (implements §3 as written). |
| 2026-08-01 | Image uploads are rejected on **header dimensions**, before any decode, and are metered against the storage quota. | Measured: a 1.5 MB all-zeros PNG decodes to 40000×40000 ≈ 2.9 GB — inside every byte cap. The pixel check ran *after* `fitz.Pixmap(data)`, in the API process, where §12's worker memory limits do not apply and an OOM kill leaves no exception to catch. Separately, normalization re-encodes to lossless PNG (3–5× the JPEG that arrived) into a namespace no quota saw. Also: `throttle_classes` had *replaced* the defaults, silently dropping the guest token/IP throttles on a guest-reachable write — now added instead. Found by the security lens. | No (implements §16/§17 as written). |
| 2026-08-01 | Claim-on-signup **moves** `uploads/…` assets from the guest prefix to the account's. | §13's "blob keys are principal-independent, so claiming is metadata-only" holds for `docs/` and `thumbs/` but not for the new `uploads/{g\|u}/{principal}/…`. Left behind, a guest's stamps sat under a prefix the claim had just marked for expiry — `guest_purge` would delete them within the hour, at exactly the moment the funnel is selling persistence. Copy-then-delete for a handful of small objects is the cheap fix; keying assets by document instead would mean inventing a document for a signature image. | No (restores §21.5's "never silently drop"). |
| 2026-08-01 | The overlay's page raster is requested with `?annots=false`. | The overlay draws every annotation itself as editable SVG. A raster that already contained them showed each one twice, and the baked-in copy did not move when the editable one was dragged. The flag is part of the thumbnail cache key so the two variants cannot collide. | No (consequence of the own-raster decision). |
| 2026-08-01 | **Custom image stamps use a hand-built appearance stream on a real Stamp annot** — the "flattened stamp" fallback is not used. | phase-03 speced both paths and said "the golden test decides on day one". It did: `test_image_stamp_renders_and_round_trips` shows the appearance-stream route renders the pixels *and* keeps a real, addressable annotation that pypdf sees and that `extract` can round-trip back to `image_stamp`. The fallback (draw into page content, track as a pseudo-annotation) would trade away interop and addressability for nothing. Mechanism: the PNG becomes an Image XObject, and the annot's `/AP /N` stream is rewritten to draw it. | No (implements the speced primary path). |
| 2026-08-01 | **An `annotate_batch` update is delete-then-add with the same `/NM`**, not a per-property mutator. | Per-type in-place mutation needs one branch per property per type (rect, colours, opacity, border, contents, quads, vertices, ink…) and still has to force an appearance rebuild afterwards. The client already sends the full spec — session batching means the draft *is* the source of truth — so re-adding from it is both shorter and uniformly correct. Creation date is carried across so "modified" stays honest. | No (implementation detail). |
| 2026-08-01 | **Text-markup quads come from our own server text layer (`GET …/text-words/`), not from PDF.js's text layer.** | phase-03 said quads come from "PDF.js's own text layer, matching what the user sees". Our overlay renders a server-rendered page raster rather than drawing on PDF.js's canvas (see next entry), so there is no PDF.js text layer to consume — and sourcing word rects from the *same PyMuPDF that applies the annotation* removes an entire class of coordinate drift instead of adding one. RTL is stronger this way, not weaker: each word carries its own rect and its own (block, line) index, so a Hebrew selection groups by visual line regardless of direction. Proven by `test_hebrew_words_are_extractable_and_highlightable` and `overlay-model.spec.ts::handles right-to-left runs`. | Phase-03 intent preserved; mechanism recorded here. |
| 2026-08-01 | **`PageOverlayComponent` renders its own page raster instead of anchoring to ngx-extended-pdf-viewer's canvas.** | The viewer owns its DOM and re-creates it on scroll and zoom, so an overlay anchored to it fights for layout on every frame and breaks whenever the viewer's internals change. Rendering the page ourselves makes the overlay's coordinate space exactly the element's box, which is what lets §8's normalized geometry hold with *no* conversion at all — and it is the same primitive phases 4/5/7/8 need. Accepted consequence: annotate mode shows one page at a time with its own zoom, and the thumbnail width cap rose from 1200 to 2000 px so placement is not guesswork on HiDPI. | No (§7 requires an overlay layer, not a particular host). |
| 2026-08-01 | Ephemeral **image assets** get their own key namespace `uploads/{g\|u}/{principal}/{ref}.png`, with a tier-resolved per-file cap and a dedicated throttle scope. | Custom stamps (P3), inserted images and image watermarks (P4) and guest signatures (P8) are not documents — no versions, no page count, no library entry — so reusing `docs/` would have meant fake Document rows. The ref is opaque and the key is **derived from the caller's principal**, so a leaked ref cannot address another principal's asset (`test_one_principal_cannot_stamp_another_principals_image`). Uploads are re-encoded to PNG, which normalizes the format for the engine and drops EXIF/GPS before it can be pasted into a file the user is about to share. Guest assets are swept by `guest_purge`. | **Yes — 01-architecture §6 URL map, §13 storage keys, §16 tier table amended.** |
| 2026-08-01 | Two MuPDF **segfault** traps, both found by tests rather than review: (a) creating or deleting a page invalidates every live `Page`/`Annot` handle; (b) `doc[i].annots()` lets the temporary `Page` be collected while its annots are still in use. | Getting an Image XObject into a PDF requires inserting it on a page, so the naive image-stamp implementation created a scratch page *while holding* the annot it was about to modify — a hard crash, not an exception. Fix: embed every image up front, before any annotation work, so no handle spans a page mutation. (b) is subtler: `annot.parent` is a **weak** reference in PyMuPDF, so the idiomatic-looking `for a in doc[i].annots()` reads freed memory. Both are now commented at the call site, because the next phase to touch annotations will otherwise rediscover them the same way. | No (implementation traps, documented in code). |
| 2026-08-01 | `storage.list_prefix` / `delete_prefix` added to both storage backends. | `guest_purge` must leave nothing orphaned, and thumbnails are keyed `thumbs/{doc}/{seq}/p{n}@{w}.png` — the page/width combinations actually rendered are not recoverable from the DB rows, so deleting only each version's `storage_key` would leave every thumbnail in the bucket forever. | No (implements §21.4's "nothing orphaned"). |

## Blockers

_(None yet.)_

## Session log — Phase 2B (2026-08-01)

**➡ Handoff — Phase 3 is next.** Start at [phase-03-annotations.md](phase-03-annotations.md); it builds the shared overlay layer (`PageOverlayComponent`, §7) reused by phases 4/5/7/8, and is unchanged by 2B except that annotation authorship must now use the principal model. `prompt-2-phases-03-07.md`'s precondition banner can be updated to **"Phases 0–2B are ✅"**.

What Phase 3 must know about the 2B refactor:
- **Ownership goes through `apps/core/principals.py` and nowhere else.** New querysets use `owned_by(qs, principal)`, new rows use `owner_kwargs(...)` / `job_owner_kwargs(...)`, worker code uses `principal_of(job)`. A grep test fails the build on `request.user`, `job.user`, `owner=` or `context["request"].user` outside that module — including in new code.
- **New endpoints are guest-accessible by default.** `IsPrincipal` is the project default permission; an account-only endpoint must say so with `IsAccount` **and** carry a written reason in §21.3.
- **New limits belong in `settings.TIERS` and resolve through `core.limits.for_principal()`.** Nothing hardcodes a limit at a call site. Note that `override_settings(MAX_UPLOAD_MB=…)` no longer changes behaviour — override `TIERS` (helper: `test_ingest.tiers_with`).
- **New tool pages append to `TOOL_PAGES` in `frontend/src/app/core/tool-pages.ts`.** Routes, prerendering and `sitemap.xml` are all generated from that table, and tests fail if they drift. Phase 3's slug is `/annotate-pdf`.
- **Anything metered goes in `METERED_OPS`, not the `heavy` queue.** They are deliberately different sets.
- Two Phase-2B acceptance criteria are **carried into Phase 8** (guest `self_sign`, guest sign-request → `account_required`) because those features do not exist yet. They are a GATE on Phase 8 sign-off.

**Preflight.** Cleared the planning session's leftovers: stale `.git/index.lock` (0 bytes, 2026-07-31 19:54; the only `git` process running was the long-lived `fsmonitor--daemon`, not a writer) and the empty `development-plans/_to_delete/`. All uncommitted paths confirmed under `development-plans/`. Toolchain: git 2.50.1, Docker 29.4.1, `gh` authenticated.

**Shipped.** Backend: `core.GuestSession` (256-bit token stored hashed, 24 h sliding TTL capped at 72 h); `Document`/`Job`/`UsageCounter` repointed to a principal with an exactly-one-of DB `CheckConstraint`; `User.plan`; `apps/core/principals.py` as the sole ownership expression; `core/limits.py` with `for_principal()` and `METERED_OPS`; `PrincipalAuthentication` + `IsPrincipal`/`IsAccount` + `account_required`/`guest_expired`/`captcha_required`; the worker layer routed through `principal_of(job)`; storage accounting moved off the `users` table; **`MAX_PAGES` enforced for the first time**; tier-resolved concurrency; `POST /api/guest/session/` and `POST /api/guest/claim/` plus inline claim on register/login; guest throttles keyed on token *and* salted IP; a Turnstile adapter gated on `METERED_OPS` only; hourly `guest_purge`. Frontend: `GuestFacade`, the interceptor credential branch, guest-aware 401/410, `accountGuard` in place of the app-wide guard, the viewer's `httpHeaders` bypass, session/claim/upgrade banners, seven public prerendered tool pages, and a generated `sitemap.xml`/`robots.txt`.

**Bugs this phase found and fixed (each caught by a test, not by review):**
1. **Parallel first-writes minted two sessions** — a guest dropping two files on `/merge-pdf` got them in different sessions, so the merge saw one. Fixed with `GuestFacade.ensureSession()`. Caught by e2e.
2. **Claim pre-flight measured the wrong number** — current-version sizes rather than the session's storage counter, so a session with edit history could pass pre-flight and still overflow the account. Caught by a failing claim test.
3. **The grep gate had a fourth hole** — `self.context["request"].user` matched none of the three named patterns once string literals were blanked out; and the allowlist already had 5 stale exemptions on its first run.
4. **Throttle counters were per-process** — no `CACHES` was configured, so DRF's throttles (and the new metered windows) used LocMem, meaning each API worker enforced a private limit.
5. **`TokenService` threw during prerendering** — unguarded `localStorage` access, invisible until SSR was switched on.

**Self-review pass (Step 8) — two further findings, both fixed on the branch:**
6. **`owned_by()` raised `FieldError` on account-only models.** `Folder` has no `guest_session` column, so `owned_by(Folder.objects.all(), guest)` was a 500 — reachable by a guest simply passing `folder=<id>` on upload. Fixed at the choke point (`accepts_guests()` → `qs.none()`), so it now protects every account-only model including the Phase-8 esign ones. `test_principals.py::test_owned_by_returns_nothing_for_a_guest_on_an_account_only_model`, `::test_guest_uploading_into_a_folder_gets_404_not_500`, `::test_owns_is_false_for_a_guest_against_an_account_only_model`.
7. **Operations minted a session *before* the ownership check.** `POST …/operations/`, `…/revert/` and `/api/operations/` all passed `write=True`, so anyone POSTing at random document ids created a `GuestSession` row per probe and then got a 404 anyway. Every one of those endpoints targets a document the caller must already own, so none of them needs to mint — only upload (and the explicit endpoint) does. `test_guest.py::test_probing_document_ids_never_mints_a_session`.

Final numbers after the review fixes: backend **232 passed**, frontend **40**, e2e **7**; `apps` coverage **91%**; schema still 0 warnings/0 errors.

**Baseline.** First full run was red on **e2e `phase-2.spec.ts`** — `[data-test=confirm-ok]` never appeared after `op-delete`. Re-ran the spec alone (green) and the whole suite again (3/3 green), so it is a **flake, not a broken base**: the spec clicks `organize-page` `nth(0)` immediately after the rotate toast, and the `viewer.reload()` → `pages.clear()` effect can wipe that selection mid-click, in which case `op-delete` shows "Select one or more pages first" instead of the confirm dialog. Slower on the session's first run because `ng serve` compiles cold. Not fixed here — it belongs to Phase 2 and this phase does not own it; logged in the Human review queue instead.

## Session log — Phase 3 (2026-08-01)

**Shipped.** Backend: `pdf_engine/engine/annotations.py` (extract / apply-ops / flatten) and `engine/text.py::page_words`; `annotate_batch` + `flatten` in the registry with per-type JSON Schemas; `GET …/annotations/` and `GET …/text-words/`; `core/assets.py` + `POST /api/uploads/image/` for ephemeral image assets; `geometry.py` gains the point pair. Frontend: `shared/page-overlay/` — the generic overlay primitive phases 4/5/7/8 will reuse — plus `AnnotationsFacade`, annotate mode (palette, page navigation, zoom, comments sidebar, save/autosave/flatten), the `/annotate-pdf` tool page, and crop migrated onto the overlay.

**Bugs this phase found, each caught by a test rather than by review:**
1. **Two MuPDF segfaults, not exceptions.** Creating/deleting a page invalidates every live `Page`/`Annot` handle (the image-stamp path did exactly that); and `doc[i].annots()` lets the temporary `Page` be collected while its annots are still in use, because `annot.parent` is a weak reference. Both are commented at the call site.
2. **A `/Text` note yields two `/Annots` entries** — itself and its `/Popup`. The first version of the golden assertion counted raw entries and failed; counting non-Popup entries is what the criterion actually means.
3. **A signed-in user's unsaved marks were attributed to "Guest."** The author is stamped server-side, so a local draft has none and the fallback was wrong for accounts. Found by reading the e2e failure snapshot.
4. **The overlay raster was capped at the 1200 px thumbnail limit**, making precise placement guesswork on HiDPI. Cap raised to 2000 and the client request clamped to it.
5. **An off-by-one in the ink point cap** — thinning then appending the endpoint produced 2001 points against a stated cap of 2000. Replacing the last kept sample fixes it and keeps the pen-up point.
6. **The e2e note step was a genuine test-synchronisation bug, not app breakage**: the palette sets the tool, which reaches the overlay as an input, and the click fired first. The spec now waits for the observable consequence (the text layer is gone). The same debugging pass showed the default 720 px viewport cannot reach the lower half of a rendered page — earlier drags only worked because pointer capture retargets off-screen events.

**Not automated (recorded, not hidden):** the 30 s autosave timer and the native `beforeunload` prompt. Playwright cannot assert a native unload dialog, and a 30 s wall-clock wait is a flake generator; the composer both paths call is unit-tested instead.

**Final numbers:** backend **314**, frontend **62**, e2e **10**; `apps` coverage **91%**, `pdf_engine` **91%**; schema 0 warnings / 0 errors.

**➡ Handoff — Phase 4 is next.** `shared/page-overlay/` is the layer to build on: map your model onto `OverlayItem`, consume `OverlayDraft`, and never fork it. `GET …/text-words/` already exists and phase-04's `text-blocks` read model belongs beside it in `engine/text.py`. `POST /api/uploads/image/` already provides the image refs `add_image`/`replace_image`/image watermarks need — resolve them worker-side with `core.assets.load_images(principal_of(job), refs)`.

## Session log — Phase 4 (2026-08-02)

**Shipped.** Backend: `pdf_engine/engine/content.py` with 17 operations (`edit_text`, `add_text`, `whiteout`, `find_replace`, `add_image`/`replace_image`/`delete_image`, `add_link`/`edit_link`/`delete_link`, `header_footer`, `page_numbers`, `bates`, `watermark`, `overlay_pdf`, `set_metadata`, `set_bookmarks`), the `text-blocks`/`images`/`links` read models, and `pdf_engine/colors.py` shared with Phase 3. Frontend: `EditFacade` and Edit mode (Edit / Stamps / Info tabs) built on the Phase-3 overlay unchanged, plus three tool pages.

**Found by tests, not review:**
1. **The coordinate family is inconsistent** — `insert_htmlbox` applies page rotation, its siblings do not. Measured across all five writers before committing to a rule.
2. **`search_for` cannot do case-sensitive search at all**, which the phase doc assumes it can.
3. **Ligatures break search on edited text** — and, it turns out, on any LaTeX/InDesign PDF the product has ever opened.
4. **A find & replace dry run was minting a version**, putting "Replaced 0 matches" in the history every time somebody searched. It now returns a report and touches nothing.
5. **The two halves of find & replace answered different result shapes**; unified so a client cannot read the wrong key.
6. **An SVG shape with `fill="none"` is only clickable on its 1px border** — text blocks were effectively unselectable. Fixed in the shared overlay, so Phase 3 inherits it.
7. **The overlay's loading placeholder intercepted pointer events.**

**Final numbers:** backend **438**, frontend **82**, e2e **17**; `apps` coverage **92%**, `pdf_engine` **92%**; schema 0 warnings / 0 errors.

**Self-review (Step 3.7) — eleven further defects, all fixed on the branch.** The regression lens came back clean; the other three found them. The two that mattered most were both about rotated pages: `search_for` answers in *unrotated* space (the code asserted the opposite), which broke find & replace, `match_case`, the review-list context and the replacement's style on any rotated page — **and had been silently breaking Phase 1's find bar since it shipped**; and every text edit on a landscape-rotated page failed with `text_overflow` telling the user their text was too long. Also: adjacent occurrences were silently deleted ("banana" + "na" reported one match and dropped one), a same-length-but-wider replacement ("cat"→"CAT") aborted the whole job, `get_image_rects` decoded every embedded image in the API process (1.1 MB PDF → 1.7 GB) and multiplied duplicate placements N², all three read models returned 500 for ordinary print PDFs, `watermark.rotation` was silently snapped to 90° steps so the spec'd −45° rendered vertical, `fits_at_size` never reached the client, and staged edits were keyed by a per-page block number so editing page 2 destroyed page 1's edit.

**➡ Handoff — Phase 5 is next.** The overlay takes field rectangles the same way it took annotations and text blocks: map to `OverlayItem`, consume `OverlayDraft`. `flatten` already supports `what=form` and is tested (`test_flatten_form_leaves_annotations_alone`). `core.assets` provides image refs if the form builder needs them. Note for Phase 6: flipping the scanned-page gate on is a single input — `<app-edit [ocrAvailable]="true">` — plus wiring `ocrRequested` to the OCR dialog.

## Human review queue

| Added | Item | Phase | GATE? | Resolved |
|---|---|---|---|---|
| 2026-07-19 | **Workspace & dashboard look-and-feel** — functional Tailwind UI, but visual polish (spacing, empty states, responsive breakpoints, brand) deserves a designer's eye. | 1–2 | No | ⬜ |
| 2026-07-19 | **Crop UX** — implemented as a margin dialog (trim %), not the draggable overlay-rectangle from phase-02. The overlay-layer primitive (`PageOverlayComponent`) is properly built in Phase 3; revisit crop to use it then. | 2 | No | ✔ **Resolved 2026-08-01 (Phase 3)** — the margin dialog is gone; Crop now drags a rectangle on the overlay and applies to the Organize selection (or all pages). `e2e/phase-3.spec.ts::crop is drawn on the overlay, not typed into a dialog`. |
| 2026-07-19 | **web dev-server memory** — `ng serve` (esbuild + pdf.js + Tailwind oxide) can OOM on an 8 GB Docker VM under concurrent load; mitigated by `restart: unless-stopped` + a web-ready wait in `test.sh`. Consider raising Docker Desktop RAM, or precompiling Tailwind to static CSS, for smoother local dev. | 0 | No | ⬜ |
| 2026-07-19 | **Host port remap on this machine** — an unrelated app occupies :8000/:5432 here, so `.env` sets API_PORT=8010, DB_PORT=15432. A clean machine gets the plan's 8000/5432 defaults. Nothing to fix; just be aware when browsing the API directly. | 0 | No | ⬜ |
| 2026-07-31 | **Guest TTL tuning** — 24 h sliding / 72 h cap is a judgement call made without usage data. Revisit with real storage numbers before launch; shorter is cheaper and a stronger privacy claim, longer is friendlier mid-task. | 2B | No | ⬜ |
| 2026-07-31 | **SSR added mid-project** — `@angular/ssr` on an existing zoneless v22 app can surface hydration mismatches. The plan deliberately SSRs only tool/landing pages and leaves the viewer client-rendered; validate that split early in 2B rather than at the end. | 2B | No | ⬜ |
| 2026-07-31 | **Turnstile account + keys** — needed before `CAPTCHA_ENABLED=true` in any public deploy. Owner-executed. | 2B/9 | No | ⬜ |
| 2026-08-01 | **Guest `self_sign` + guest sign-request `account_required`** — the two Phase-2B acceptance criteria that test features which do not exist yet. Must be demonstrated when Phase 8 lands them. GATE for Phase 8 sign-off. | 2B/8 | **Yes** | ⬜ |
| 2026-08-01 | **`mypy` (98 errors) and `eslint` (no target configured)** — both §20 DoD items are unmet repo-wide and always have been. The mypy fix is one `pyproject.toml` change (enable the django-stubs plugin) plus whatever it surfaces; eslint needs `ng add @angular/eslint` and a first cleanup pass. Suggested owner: Phase 10 hardening. | 0–10 | No | ⬜ |
| 2026-08-01 | **Docker VM memory vs the SSR build** — building browser + server bundles OOM-killed (exit 137) on the 7.8 GB VM while all 11 containers were up; it succeeds with the workers/gotenberg stopped. Raising Docker Desktop RAM would remove the constraint. Related to the existing web dev-server memory item. | 2B/0 | No | ⬜ |
| 2026-08-01 | **`e2e/phase-2.spec.ts` is flaky** — the delete step races `viewer.reload()` → `pages.clear()`, which can wipe the page selection between the click and `op-delete`, so the confirm dialog never opens. Failed once, then passed on two consecutive re-runs. Belongs to Phase 2; not fixed in 2B. Fix is to re-assert the selection after the preceding toast (or have the spec wait for the reloaded organize grid). | 2 | No | ✔ **Resolved 2026-08-01** (`fix/phase-2-e2e-flake`) — spec-only: waits for the reloaded grid (header `v2 · N pages`) after the rotate toast, then re-asserts `1 selected` before clicking `op-delete`. Proven by 3 consecutive green runs of the spec alone plus the full suite. |
| 2026-08-01 | **Annotations in a third-party viewer — one manual check.** Phase 3's first acceptance criterion asks for a look in Preview/Acrobat, and is ticked `[~]` for exactly that reason. Mechanized as far as it can be (a *second* library, pypdf, finds every annotation type in `/Annots` on the downloaded bytes), but "looks right in Acrobat" is a rendering judgement no test makes. Worth five minutes with an exported file. Known nuance to look at while there: an image stamp on a *rotated* page is placed correctly but inherits the page's rotation. | 3 | No | ⬜ |
| 2026-08-01 | **Account-side cleanup of `uploads/…` image assets.** Guest assets die with the session; an account's uploaded stamps/watermarks/signatures currently persist. They are bounded by a per-file tier cap and a dedicated `image_upload` throttle scope, so this is housekeeping rather than an abuse hole. Natural owner: phase-09 "Storage hygiene" alongside the exports GC. | 3/9 | No | ⬜ |
| 2026-08-02 | **Text-edit fidelity on exotic fonts** — the replacement font is a substitute whenever the original is a subset, which is nearly always. Body text looks right; a display or branded font will be visibly different. The constraint is stated in the UI and in the `/edit-pdf` copy, and the block-scoped model keeps the blast radius to one block. Worth an eyeball on a real branded document before launch. | 4 | No | ⬜ |
| 2026-07-31 | **Ad-revenue assumption unvalidated** — the whole strategy rests on organic tool-page traffic converting to ad impressions at a viable RPM. Worth a sanity check against real AdSense figures for utility sites before committing further engineering to the ad path. | 9 | No | ⬜ |

## Session log

_(Newest first.)_

**2026-07-31 — Session 2 (planning only, no code) — Access model re-planned**

- Owner questioned the login gate. Outcome: **access model changed to anonymous-first**; paid tier stays out of v1 but limits are now tier-resolved so billing is addable without a refactor. Full rationale in the Decisions log above.
- **Plan docs amended (no application code touched):** `01-architecture.md` §§1, 6, 7, 9, 10, 11, 13, 15, 16, 17, 19, 20 + **new normative §21 (Access model)**; **new** `phase-02b-anonymous-access.md`; `README.md` (locked decisions, index, dependency graph, ordering); `02-feature-matrix.md` (access-tier section + platform rows); `phase-00` + `phase-01` (retrofit banners + superseded criteria), `phase-03`–`phase-07` (guest-parity + tool-page acceptance criteria; phase-03 guest annotation authorship), `phase-08` (self-sign guest / requests account-only), `phase-09` (rescoped — abuse controls and tool pages moved out; throttle matrix defers to §16), `phase-10` (isolation sweep covers guests), `prompt-2-phases-03-07.md` (stale-precondition banner — **do not run it until 2B lands**).
- **Adversarial review pass run over the amended plan** (independent agent, read against the *built* code). 18 findings, all folded in — the load-bearing ones: the worker layer resolves ownership through `job.user` not `request.user` (so the grep-test gate had to widen, and `owner=None` would both violate the new constraint and match any guest's docs); storage accounting is `User`-only in `services.py`/`tasks.py`/`views.py`; **`MAX_PAGES` is not enforced anywhere today** despite §17 claiming it is; claim had to cover jobs + usage counters, not just documents; "heavy op" meant three different things and the collision would have put a CAPTCHA in front of `/merge-pdf`; the viewer's `httpHeaders` and `pdf-thumbnail.ts` bypass the HTTP interceptor entirely; and the absolute "no guest-triggerable email" claim was false (Phase 8 mails on ceremony actions) — narrowed to *address selection*.
- **Status unchanged for Phases 0–2** — they remain ✅; 2B retrofits them rather than reopening them. Their acceptance criteria must still pass for authenticated users after the refactor (2B acceptance criterion 10).

**➡ Handoff — Phase 3 is no longer next. Start at [phase-02b-anonymous-access.md](phase-02b-anonymous-access.md).** Read 01-architecture **§21 first** (it is normative and supersedes the login-gated assumptions still embedded in phase-01's prose). The refactor's blast radius is ownership: `apps/core/principals.py` becomes the only place ownership is expressed, and a grep-based test enforces it. Phase 3 (annotations/overlay layer) follows 2B unchanged.

**2026-07-19 — Session 1 (agent, Prompt 1: Phases 0–2) — COMPLETE**

- **Phases 0, 1, 2 all ✅** with evidence (sections above). Full stack builds & runs on a torn-down volume-wiped state.
- **Verification (green):** `./infra/reset.sh --yes && ./infra/up.sh && ./infra/test.sh --e2e` →
  backend **125 passed** (coverage: apps **91%**, pdf_engine **92%** — gates 85/90 met), frontend **9 passed** (Vitest),
  e2e **3 passed** (phase-0/1/2 Playwright happy-paths). ruff + mypy(loose) clean. `manage.py check` clean; migrations idempotent from zero.
- **Seeded demo user** (`admin@zenpdf.local` / `admin12345`) can register/login, upload, view, search, organize pages, merge/split, and revert versions through the UI (proven by e2e).
- **Commits:** foundation committed (`fdb6e95`); a final commit lands the frontend, e2e, and the infra fixes from this session.
- **Bugs found & fixed during verification:** (1) revert job wasn't tracked to completion in the workspace (only created) — now uses `JobsFacade.dispatch`; (2) `test.sh` backend step inherited dev settings (pytest-django env-var precedence) — now forces `config.settings.test`; (3) default doc list didn't hide trashed docs; (4) `engine_inspect.inspect` mis-call in OutlineView.
- **Environment adaptations (this host):** ports 8000/5432 taken by an external app → parameterized host ports, `.env` uses 8010/15432; postgres:18 volume remounted at `/var/lib/postgresql`; web `--poll` dropped + `restart: unless-stopped` (esbuild/pdf.js OOM under load). All in Decisions log.

**➡ Handoff — where Phase 3 begins:** ⚠ *Superseded 2026-07-31 — **Phase 2B now comes first**; see the newest session-log entry above. The rest of this note remains accurate for Phase 3 once 2B lands.* Start at `development-plans/phase-03-annotations.md`. Phase 3 builds the **shared overlay layer** (`PageOverlayComponent`, arch §7) — the positioned div-stack over each rendered page reused by phases 4/5/7/8. The workspace already has the viewer, thumbnail rail, tool-panel scaffold, and organize grid to hang it on; the crop tool (currently a margin dialog) should be migrated onto the overlay primitive once built. Backend: add `annotate_batch` + `flatten` engine fns (`pdf_engine/engine/annotations.py`) and register in `pdf_engine/registry.py` (queue `default`); the operation pipeline, versioning, geometry (§8), and job UX are all in place and reusable. **Human review queue** has 4 non-blocking items to eyeball (workspace look-and-feel, crop UX, web dev-server memory, host port remap).

**2026-07-19 — Session 1 (agent, Prompt 1: Phases 0–2)**
- Preflight: git 2.50.1 (repo init'd), Docker 29.4.1 + Compose v5.1.3 (daemon healthy, 8.3 GB, linux/arm64), Node 25 host → scaffolding done via `node:24` container. No `django-angular-project-setup` skill installed locally; following arch §4/§6/§7 conventions verbatim.
- Built `infra/docker/api.Dockerfile` (py3.14-slim ARG-parameterized) → engine stack validated (scaffold item 5). One pin fix (pymupdf4llm). Import smoke test green.
- Scaffolded Angular 22 app via `node:24` (`ng new … --skip-install`); default runner = Vitest 4.
- Wrote Django config/settings (base/dev/prod/test), top-level URLs, core exception handler (error shape §6). Building out apps next.
- Note: Docker Desktop sits behind a flaky HTTP proxy (192.168.65.1:3128) — transient image-pull timeouts; retried with backoff.
