# ZenPDF

All repo-local agent rules live in `AGENTS.md` — read it before working here.

@AGENTS.md

In particular: any change that touches UI elements or any client-facing surface (components, templates, styles, copy, assets, emails) MUST follow the design contract at `docs/design/design-instructions.md` — see "Design governance" in `AGENTS.md`.
